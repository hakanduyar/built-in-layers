# PROGRESS — Built in Layers

How to use this file: every task appends a log entry when it starts and when it stops, and keeps the status table truthful. Deferred scope discovered mid-task is recorded here, never silently implemented.

## Status

Execution order revised 2026-07-17 (D-011 rejected): content system (TASK-004) now precedes the homepage (TASK-003).

| Phase | Task | Status | Review verdict |
|---|---|---|---|
| 0 | Preconditions | **Complete** — git initialized; decisions resolved; initial docs commit made 2026-07-17 (`91aaf9c`) | — |
| 1 | TASK-001 Foundation (+ D-001 MDX spike, D-003 font verification) | **Technically complete 2026-07-27** — full gate suite passes incl. Chromium + WebKit; **D-001 ACCEPTED FOR MVP 2026-07-29** (KEEP `next-mdx-remote/rsc`, approved with binding conditions) | Approved |
| 2 | TASK-002 Static shell (desktop inline nav + mobile MENU panel) | **Approved and committed 2026-07-29** (`f4f7a69`, "feat: implement static shell") | Approved |
| 3 | TASK-004 Content system | **COMPLETE (2026-08-10)** — infrastructure checkpoint committed and pushed 2026-07-29 (`bd6dd3b`); all 7 acceptance criteria now genuinely met, including the reconciled preview-depth-superseded criterion (verified against all four published projects) — see `docs/tasks/TASK-004-content-system.md` | Complete |
| 4 | TASK-003 Homepage static (loader-fed; static-layout approval gate) | **COMPLETE (2026-08-10, homepage-image completion pass)** — full gate suite green (Chromium + WebKit); **9 of 9 acceptance criteria now genuinely met** — `ProjectCard` (shared by `/` and `/work`) now renders each project's real, first registered `images[]` entry as a D-019-compliant representative asset — awaiting review | — |
| 5 | TASK-005 Kıvılcım case study | **COMPLETE** (2026-08-05) — `status: "published"`, `verificationStatus: "verified"` under **D-019** (`docs/DECISIONS.md`). Text was approved 2026-08-03 via Hakan's explicit delegation to the assistant, four accuracy corrections applied; the project then stayed `status: "draft"` from 2026-08-03 to 2026-08-05, blocked solely on missing real screenshots (see the dated log entries below for that history) — D-019 resolved the block by permitting honestly-labelled `verified-diagram`/`provisional-illustration` assets in the interim. Real screenshots remain a wanted future replacement, not a publication blocker. | Complete, awaiting final review |
| 6 | TASK-006 DropSpot case study | **COMPLETE** (2026-08-05, remediated same day) — `status: "published"`, `verificationStatus: "verified"` under **D-019**. Repository audit performed directly against `https://github.com/hakanduyar/dropspot-project` (read-only clone, deleted after each pass); every technical claim (row-locking, transactions, priority formula) verified line-by-line in source, not taken from the README. `depth: "short"`. Same-day remediation replaced the four D-019 diagrams' fourth member (`screens-map.svg`, removed) with 4 real repository screenshots (`assetType: "real-screenshot"`), re-inspected directly rather than trusted from filenames/README captions — two of which turned out to be mislabelled in the source repo itself. | Complete, awaiting final review |
| 7 | TASK-007 Motion layer + explorer | **COMPLETE (2026-08-11)** — reviewed and approved. `motion` installed; Layer Explorer is an accessible WAI-ARIA Tabs interface (manual activation, D-006); restrained site-wide reveals; **7 of 7 acceptance criteria genuinely met** after fixing two real bugs found via adversarial review + empirical browser tracing (see dated log entries) | Approved |
| 8 | TASK-008 A11y / performance / SEO | Not started | — |

Planning documents: **conditionally approved 2026-07-17; required revisions applied same day. TASK-001 awaits start approval.**

## Open items blocked on Hakan

- **Historical (2026-07-29): Kıvılcım, DropSpot, and JointLedger all carried provisional development-placeholder metadata** — none of it a user-verified fact at that time. To let implementation continue without blocking on Hakan, `phase`, `aiAssisted`/`aiDisclosure`, and `contribution` were set to explicit placeholder values on all three (see exact values in the task report), and all three stayed `status: "draft"` while those values remained provisional.
- **Current (2026-08-05): this no longer applies to Kıvılcım or DropSpot.** Kıvılcım's repository audit (TASK-005, 2026-07-31), delegated text approval (2026-08-03), and D-019 publication (2026-08-05) replaced every placeholder value with a real, verified one. DropSpot's repository audit and publication (TASK-006, 2026-08-05) did the same directly in one pass — `contribution` is derived from git-history evidence (100% solo-authored, no forks), `aiAssisted` is left unset rather than guessed (no repository evidence either way), and `status: "published"` / `verificationStatus: "verified"` reflect real, checked facts, not placeholders. See the dated log entries below for the full history.
- **Current (2026-08-10): JointLedger is no longer draft either.** A direct repository audit (both `hakanduyar/jointledger` — main and the unmerged `feature/shared-family-book` branch — and upstream `mayswind/ezbookkeeping`) replaced every remaining placeholder: `upstream.relationship` is now a real, evidence-based description; `contribution` distinguishes upstream ezBookkeeping from Hakan's own Book/BookMember work and explicitly excludes what he didn't build; `aiAssisted` stays omitted (no repository evidence either way, same D-018 pattern as DropSpot). `status: "published"`, `verificationStatus: "verified"`, `depth: "short"`. No project remains draft on the D-016 list. See the dated log entry below for the full audit.
- **JointLedger's upstream is genuinely confirmed** (not a placeholder): `upstream.url: "https://github.com/mayswind/ezbookkeeping"`, `upstream.name: "ezBookkeeping"` — and, as of this pass, actually rendered wherever a fork-provenance project appears (`CaseStudyHero`/`ProjectCard`), not just present in frontmatter.
- **Professional Systems is unaffected by the above** — `phase` and `aiAssisted` remain omitted (D-018; no approved value exists), `status` stays `published`, and its content is unchanged: the exact D-009 wording, zero concrete claims.
- Public email, current CV, current location: remain unpublished until explicitly confirmed (never taken from old CV files).
- Notes: selection of three Medium articles (title + URL).
- Assets: real screenshots for Kıvılcım and for DropSpot — both are wanted future replacements for their current D-019 diagrams, no longer publication blockers for either (DropSpot's own repository already contains 8 real screenshots + 1 schema diagram, not yet imported — see `docs/CONTENT_INVENTORY.md`); OG default image direction (needed by TASK-008).
- Production domain (required before TASK-008 completion — not a blocker for earlier phases).

## Resolved 2026-07-17 (conditional approval batch)

- Git repository initialized (removes the "not a git repository" conflict with `CLAUDE.md §13`).
- Decision batch: D-001 provisional (spike in TASK-001, `@next/mdx` fallback), D-002 accepted, D-003 accepted provisionally, D-004 accepted with changes (`--signal-ui`/`--signal-text`), D-005 rejected → mobile MENU panel, D-006 accepted with manual activation, D-007/D-008 accepted, D-009 accepted with exact wording, D-010 revised (Chromium + WebKit), D-011 rejected → task reorder, D-016 accepted (Kıvılcım, DropSpot, JointLedger, Professional preview), D-017 Kıvılcım naming.
- Kıvılcım display name: **Kıvılcım** (first English introduction may gloss: Kıvılcım — “Spark”); slug `/work/kivilcim`.
- Homepage featured order and its promotion condition (D-016).
- Email/CV/location policy for MVP: omitted until confirmed.

## Deferred scope

*(empty)*

## Log

### 2026-07-17 — Planning phase (documentation only)

- Created: `ARCHITECTURE.md`, `DESIGN_SYSTEM.md`, `CONTENT_MODEL.md`, `ROADMAP.md`, `DECISIONS.md`, `PROGRESS.md`, `QA_CHECKLIST.md`, `tasks/TASK-001`–`TASK-008`.
- No production code written, no packages installed, no commits made (not a git repository yet at that time).
- Conflicts found and reported in the planning summary: missing git repo vs CLAUDE.md §13; PROJECT_SPEC §8 featured order vs Professional Systems `REQUIRES USER` status (→ D-016); duplicate `medyanes-smartboard-app` / `medyanes-smatboard-app` entries in CONTENT_INVENTORY.
- Status: **STOPPED — awaiting approval. TASK-001 not started.**

### 2026-07-17 — Planning revision (documentation only, per conditional approval)

- Applied Hakan's 18 approval decisions across: `PROJECT_SPEC.md`, `CONTENT_INVENTORY.md`, `ARCHITECTURE.md`, `DESIGN_SYSTEM.md`, `CONTENT_MODEL.md`, `ROADMAP.md`, `DECISIONS.md`, `QA_CHECKLIST.md`, `PROGRESS.md`, all eight task files.
- Key changes: D-001 provisional with a mandatory TASK-001 MDX compatibility spike; D-005 replaced with desktop-inline / mobile-MENU navigation (fixed client-component count removed); D-006 manual activation; D-011 rejected — execution order now 001 → 002 → 004 → 003 → 005 → 006 → 007 → 008; build-gate language honest ("reduce the risk", not "structurally impossible"); `verificationStatus` field made explicit; `--signal-ui` token added; Playwright Chromium + WebKit; Kıvılcım naming fixed (D-017).
- Git repository initialized (no commit). No production code written, no packages installed, TASK-001 not started.
- Status: **STOPPED — revisions complete, awaiting approval to begin TASK-001.**

### 2026-07-27 — Documentation baseline commit

- Committed the approved planning/governance file set only (`CLAUDE.md`, the four root prompt files, all of `docs/`) as `91aaf9c` "docs: establish portfolio project plan". Remote `origin` set to `https://github.com/hakanduyar/portfolio.git`; push attempted but blocked by missing git credentials in the sandbox (reported separately; not resolved in this session).

### 2026-07-27 — TASK-001 Foundation implemented

- Scaffolded Next.js 16.2.12 / React 19.2.4 / TypeScript 5.9.3 / Tailwind v4.3.3 workspace (via an isolated temp-dir `create-next-app` run, then hand-ported into the repo root, since the repo's existing root files fail create-next-app's own empty-directory check).
- Environment fix (not a project file): global `~/.npmrc` pointed at an unreachable private registry (`registry.hmb.gov.tr`); added a project-local `.npmrc` pinning `https://registry.npmjs.org/` without touching the global config.
- Implemented full `styles/globals.css` token set (colors, type roles, tracking, radius, motion durations/easings, container width, focus style, reduced-motion CSS) per DESIGN_SYSTEM §2–§7, §13. Verified precise WCAG contrast ratios by computation (see D-004 addendum) rather than relying on the docs' approximations.
- Configured and verified all three fonts (Archivo, Newsreader italic, IBM Plex Mono) via a real production build, not just typechecking; confirmed self-hosted `.woff2` delivery and correct CSS variable wiring in the generated output. Licensing re-verified against the canonical `google/fonts` source repo. See D-003.
- Ran the mandatory D-001 MDX compatibility spike: all 8 required checks passed against the real toolchain; evidence recorded in `docs/DECISIONS.md`; spike artifacts removed after recording, gates re-run clean. Surfaced one new risk not previously known: the `next-mdx-remote` GitHub repo is archived upstream (does not block the decision; flagged for awareness).
- Full gate suite: typecheck, lint, format:check, unit tests, and production build all pass. Playwright e2e: Chromium passes; WebKit cannot launch in this sandbox due to missing root-only system libraries (see "Open items blocked on Hakan").
- No commit made. No files outside TASK-001's scope touched.
- Status: **STOPPED — TASK-001 implemented and verified (WebKit e2e excepted, environment-blocked). Awaiting review for Phase 1.**

### 2026-07-27 — TASK-001 final remediation and documentation update

- Re-ran the full verification suite fresh on the real development machine: `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test` (3/3), `pnpm build`, and `pnpm exec playwright test` — **all pass, including both Chromium and WebKit** (the earlier sandbox-only WebKit blocker is resolved; confirmed by an actual run, not assumed).
- Audited `.npmrc`: single line, `registry=https://registry.npmjs.org/` — no tokens, usernames, passwords, or secrets of any kind; scope confirmed limited to overriding the machine-global unreachable private registry for this project only.
- Audited `pnpm-workspace.yaml`: `allowBuilds` traced with `pnpm why` to confirm both entries are genuinely required — `sharp` is a direct dependency of `next` itself (image optimization), `unrs-resolver` is a transitive dependency of `eslint-config-next` (via `eslint-import-resolver-typescript`/`eslint-plugin-import`). No unrelated packages granted build permission. `minimumReleaseAgeExclude: [jsdom@30.0.0]` confirmed still technically necessary: jsdom 30.0.0 was published only ~6 hours before this audit — nowhere near old enough to clear any reasonable release-age policy. Not removed or broadened.
- Added the smallest reproducibility config to `package.json`: `packageManager: "pnpm@11.17.0"` and `engines.node: ">=22.23.1"` — both set to the actual tested versions on this machine, nothing broader claimed. Verified no scope creep in dependencies (no CMS/DB/auth/analytics/state/i18n/GSAP/Three.js/WebGL package; `motion` still absent, correctly deferred to TASK-007).
- **D-001 status changed to exactly `PROVISIONAL FOR MVP FOUNDATION — REVIEW REQUIRED BEFORE TASK-004`**: the passing spike confirms technical compatibility only; a focused comparison against `@next/mdx` is now a mandatory, documented precondition before TASK-004 builds real content on this pipeline. The pipeline is not to be swapped automatically on the archived-repo finding alone — migration requires comparison evidence and Hakan's explicit approval. No MDX implementation code was touched in this task.
- Status: **STOPPED — TASK-001 technically complete (full suite green, incl. WebKit). D-001 remains provisional pending the pre-TASK-004 comparison. Awaiting review for Phase 1.**

### 2026-07-29 — TASK-002 Static shell implemented (fresh implementation)

- Implemented from the approved, clean TASK-001 baseline (HEAD/`origin/main` `996ed86`, tree identical to `e01c242`) — an earlier TASK-002 implementation and its follow-up documentation commit had been reverted (two auditable `git revert` commits) after being approved in error in an unrelated conversation; neither reverted commit's content was reused or restored. This implementation was written independently against the task file.
- Created: `data/site.ts`, `data/copy.ts`, `lib/seo/metadata.ts`; `components/ui/{Container,MonoLabel,SectionHeading,TextLink,ButtonLink}.tsx`; `components/layout/{SkipLink,SiteHeader,MobileNav,SiteFooter}.tsx`; `app/work/page.tsx`, `app/notes/page.tsx`, `app/lab/page.tsx`, `app/about/page.tsx`, `app/not-found.tsx`; `tests/e2e/shell.spec.ts`.
- Changed: `app/layout.tsx` (skip link, header, `<main id="main" tabIndex={-1}>`, footer, title template per ARCHITECTURE §9), `app/page.tsx` (minimal home stub: word mark + one-line positioning only — full homepage is TASK-003), `tests/e2e/smoke.spec.ts` (h1 expectation updated to the new home stub).
- `components/layout/MobileNav.tsx` is the sole `"use client"` component (D-005). The trigger and `<dialog>` are unconditionally rendered on both server and client — no mount-gated conditional render — so there is no structural difference between the pre- and post-hydration DOM to cause layout shift. No-JS users get a `<noscript>` link list that also hides the (otherwise inert) trigger via a scoped `<style>`, resolved at HTML-parse time with no JavaScript dependency. Verified empirically, not assumed: a Playwright regression test asserts the header's bounding-box height is identical before and after hydration under an artificial 600ms delay injected into `_next/static` responses, on both Chromium and WebKit; a second regression test asserts the MENU trigger's `id` is present in the raw server-rendered HTML (checked via the navigation response body, before any script executes).
- Full verification suite: `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm build && pnpm exec playwright test` — all green. Playwright: 32/32 passing on both Chromium and WebKit (shell + smoke specs, including the two hydration regression tests, an Escape-closes/focus-restore test, a close-button test, a route-selection-closes-panel test, and a JavaScript-disabled pass confirming every nav link stays reachable and the trigger is hidden).
- Manual checks: no horizontal overflow at 375/768/1024/1440px on any of the five routes (scripted check + visual screenshot review); mobile MENU panel opens/closes correctly at 375px; footer CTA renders at the `display-l` role; skip link is the first Tab stop and moves focus to `#main` (added `tabIndex={-1}` to `<main>` so fragment-navigation focus lands reliably cross-browser).
- No new dependencies. All chrome strings sourced from `data/copy.ts`/`data/site.ts`; social links are exactly the three CONTENT_INVENTORY-verified URLs (GitHub, LinkedIn, Medium); no email/CV anywhere. `/work` and `/about` stub copy uses honest, non-D-009 wording (D-009 only defines exact text for Lab and Notes); `/lab` and `/notes` use the exact D-009 wording.
- Status: **STOPPED — TASK-002 implemented and verified. No commit made. Awaiting review for Phase 2.**

### 2026-07-27 — TASK-001 approved with metadata correction

- Hakan approved TASK-001 with one correction: `package.json` `engines.node` changed from `>=22.23.1` (the exact tested patch, overly strict) to `>=22.0.0` (the project's true minimum-compatible major, matching pnpm 11's own Node 22+ requirement). `packageManager: "pnpm@11.17.0"` unchanged. No dependencies, lockfile, or implementation code changed.
- Full verification suite re-run after the correction: typecheck, lint, format:check, unit tests, build, and Playwright (Chromium + WebKit) — all pass.
- Commit `chore: establish portfolio foundation` created and pushed to `origin/main`.
- Status: **TASK-001 approved and committed. TASK-002 not started.**

### 2026-07-29 — D-001 mandatory MDX pipeline comparison (research only, no implementation)

- Performed the mandatory pre-TASK-004 comparison of `next-mdx-remote/rsc` (Candidate A) against `@next/mdx` (Candidate B), required by D-001's binding process rule before TASK-004 may build real content on either pipeline.
- Built two isolated, fully-removed spikes (`app/spike-d001-a/[slug]`, `app/spike-d001-b/[slug]`, matching fixture content under `content/spike-d001-a|b/`) and verified both with real `pnpm build` runs, raw prerendered-HTML inspection, and standalone filesystem/gate probes. Candidate B required temporarily installing `@next/mdx@16.2.12`, `@mdx-js/loader@3.1.1`, `@mdx-js/react@3.1.1`, `remark-frontmatter@5.0.0`, `remark-mdx-frontmatter@5.2.0`, `@types/mdx@2.0.14` and temporarily editing `next.config.ts` plus adding a root `mdx-components.tsx`.
- Both candidates proved fully capable: App Router/Server Component rendering, build-time prerendering, `generateStaticParams` (draft correctly excluded from both), typed frontmatter, Zod validation, custom-component whitelisting, independent multi-file layer compilation (`index/surface/flow/system.mdx`), filesystem enumeration, and publication-gate feasibility (a raw-text `[CONTENT REQUIRED` scan, proven independent of which library compiles the MDX body — a wash between candidates).
- Found and worked around a real, previously undocumented Turbopack incompatibility in `@next/mdx`: passing `remarkPlugins` as function references fails with a non-serializable-options error; package-name strings are required instead.
- Found and cited definitive security-model evidence by reading the installed package source directly: `next-mdx-remote` (via `@mdx-js/mdx`'s `run()`) executes compiled MDX through `new AsyncFunction(String(code))(options)` — the library's own code comment reads "☢️ Danger: this evals JavaScript" — mitigated by the already-enabled `blockJS`/`blockDangerousJS` flags. `@next/mdx` (via `@mdx-js/loader`) compiles straight to static module source fed to the bundler, with no runtime `eval`/`Function` step at all.
- **Recommendation: KEEP `next-mdx-remote/rsc` for the MVP** — full evidence, side-by-side comparison table, and reasoning recorded as a D-001 addendum in `docs/DECISIONS.md`. This does **not** self-approve; the pipeline is unchanged pending Hakan's explicit approval.
- All spike files, the temporary dependencies, and the temporary `next.config.ts` edit were fully removed; the production tree was verified byte-identical to `f4f7a69` (`git status --short` and `git diff --stat` both empty) before the full verification suite was re-run clean.
- No implementation code touched. No TASK-003/TASK-004 work started. No commit made.
- Status: **STOPPED — D-001 comparison complete and documented. Awaiting Hakan's approval before TASK-004 may begin.**

### 2026-07-29 — D-001 final decision approved (documentation only, no implementation)

- Hakan explicitly approved the D-001 comparison's recommendation. **Final decision: KEEP `next-mdx-remote/rsc` for MVP.** D-001 status changed to `ACCEPTED FOR MVP — next-mdx-remote/rsc 6.0.0`.
- Binding conditions recorded in `docs/DECISIONS.md`: pin `next-mdx-remote` at exactly `6.0.0`; compile only trusted, repository-owned local MDX; keep `blockJS: true` and `blockDangerousJS: true` explicit; preserve `gray-matter` frontmatter parsing and Zod validation; preserve the published-content build gates; re-evaluate D-001 before a major Next.js/MDX pipeline upgrade, a relevant security advisory, a production architecture change, or a demonstrated compatibility failure.
- The archived-upstream maintenance risk on `hashicorp/next-mdx-remote` is **not dismissed** by this approval — recorded explicitly as an accepted and monitored MVP trade-off, standing and ongoing for as long as this pipeline is used.
- Confirmed before editing: only `docs/DECISIONS.md` and `docs/PROGRESS.md` were modified since the last commit (`f4f7a69`); no application code, dependencies, or the MDX implementation were touched by this decision.
- Full verification suite re-run clean: typecheck, lint, format:check, unit tests, production build, and Playwright (Chromium + WebKit).
- Commit `docs: finalize MVP MDX pipeline decision` created and pushed to `origin/main`.
- Status: **D-001 approved and committed. TASK-003 and TASK-004 not started.**

### 2026-07-29 — TASK-004 Content system implemented

- Implemented the typed content pipeline on the approved D-001 pipeline (`next-mdx-remote/rsc@6.0.0`, `blockJS: true`, `blockDangerousJS: true` explicit): `lib/content/schemas.ts` (Zod schemas matching CONTENT_MODEL §1–5 exactly, including all six enums and the five `superRefine` conditional rules), `lib/content/validate.ts` (marker gate, verification/depth gate, layer-meaning gate — a local Levenshtein-based similarity check, no new dependency), `lib/content/mdx.ts` (compile wrapper with an explicit, currently-empty custom-component whitelist), `lib/content/work.ts` (filesystem loader: `getAllProjects`, `getPublishedProjects`, `getProjectsByTier`, `getProjectBySlug`, `getProjectLayers`, `getProjectIndexBody`, all accepting an optional `contentDir` override for testability).
- Content: `content/work/{kivilcim,dropspot}/{index,surface,flow,system}.mdx` (layer files are `[CONTENT REQUIRED]` skeletons, not loaded — both `depth: "preview"`), `content/work/jointledger/index.mdx`, `content/work/professional-systems/index.mdx` (exact D-009 wording). `data/notes.ts` (empty array — D-008, no articles confirmed yet).
- Routes: `app/work/page.tsx` now lists `getPublishedProjects()` via a new `components/project/ProjectCard.tsx` (kept `h1` "Selected systems" unchanged so the existing TASK-002 shell tests still pass); `app/work/[slug]/page.tsx` (new) with `generateStaticParams`/`dynamicParams = false`, rendering title/description/tech/links/layers-when-present, deliberately minimal per the implementing instruction's binding constraints; `app/notes/page.tsx` wired to `data/notes.ts` (output unchanged today since the array is empty).
- **Security**: `getProjectBySlug`/route slugs are never concatenated into a filesystem path — the loader only ever calls `fs.readdirSync`/`readFileSync` against `content/work/<enumerated-dir>/`, and slug lookups are an in-memory equality check against that already-enumerated list, making path traversal structurally impossible rather than filtered. `fs` read errors are caught and re-thrown naming only the project-relative file, never an absolute path.
- **Gate demonstration** (acceptance criterion): temporarily inserted a `[CONTENT REQUIRED` marker into the real, published `content/work/dropspot/index.mdx`, ran `pnpm build`, and confirmed it failed with `Error: dropspot (index.mdx / frontmatter): published content must not contain a [CONTENT REQUIRED marker`. Reverted immediately (diffed byte-identical against a pre-edit backup); `pnpm build` re-run clean afterward.
- **Deviations** (flagged before implementing, per the instruction's own binding constraints): `components/ui/Figure.tsx`, placeholder SVGs, and `components/project/{CaseStudyHero,DecisionList,NextProject,LayerSection}.tsx` were not built — no seed entry has an image, decision, next-project link, or full/short-depth layers yet, so nothing justifies them today (all four are `depth: "preview"`); they're straightforward to add in TASK-005/006 once real full-depth content exists. **JointLedger stays `status: "draft"`**, not published — see "Open items blocked on Hakan" above; this means the live `/work` order is Kıvılcım, DropSpot, Professional Systems (three items), not the four-item D-016 order, until the upstream URL is confirmed.
- Full verification suite: `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm build && pnpm exec playwright test` — all green. Vitest: 59/59 (schema conditional rules, all publication-gate rules with a negative case each, filesystem enumeration, ordering, layer ordering via `renderToStaticMarkup`, unknown-slug lookup, missing-content-directory handling). Playwright: 58/58 on Chromium + WebKit (existing shell/smoke suite unaffected + 12 new `/work` route tests: listing order, draft/unknown 404s, no leaked markers, D-017 gloss, no horizontal overflow at 375/768/1024/1440px).
- No new dependencies (`git diff --stat` on `package.json`/`pnpm-lock.yaml`: empty). No Client Components added.
- No commit made. Files touched limited to TASK-004 scope plus this progress log and the TASK-004 task file.
- Status: **STOPPED — TASK-004 implemented and verified. Awaiting review for Phase 3.**

### 2026-07-29 — TASK-004 strict review and remediation

- A strict review of the above implementation found real blocking issues: the TASK-004 acceptance criteria had been reworded to match what was built rather than left unchecked; the required 11-section case-study template and its components were never built; the CONTENT_MODEL §3 required-section heading gate was never implemented; `/work` was a flat list, not tiered; and — the most serious finding — `phase: "active-development"` and `aiAssisted: false` were asserted on published content with no approved source, which is exactly the kind of unverified claim CLAUDE.md §2 exists to prevent. Full findings are in the review transcript, not duplicated here.
- **Acceptance criteria restored** to their exact original wording (no `[~]` or other undefined status); genuinely met ones are `[x]`, the rest stay `[ ]` with an explanatory note directly beneath — see `docs/tasks/TASK-004-content-system.md`.
- **D-018 recorded**: `phase` and `aiAssisted` are now schema-optional in `lib/content/schemas.ts`, since no approved document states either value for Kıvılcım, DropSpot, or Professional Systems, and the alternative (inventing a value, or adding an unapproved "unknown" enum member) was explicitly ruled out.
- **Metadata remediated per project**:
  - **Kıvılcım**: `status` → `draft` (its featured-tier `contribution` requirement can't be honestly filled — see "Open items" above); `phase`/`aiAssisted` omitted; `contribution` is now `[CONTENT REQUIRED: Hakan's role statement — pending confirmation]`.
  - **DropSpot**: same treatment — `status` → `draft`, `phase`/`aiAssisted` omitted, `contribution` → `[CONTENT REQUIRED: ...]`; its body no longer claims repository-unverified concurrency/race-condition handling.
  - **Professional Systems**: stays `status: "published"` (CONTENT_MODEL §5 explicitly allows this because it makes zero concrete claims) — only `phase` and `aiAssisted` were removed; its `contribution` and body text are unchanged (already zero-claim, not flagged by the review).
  - **JointLedger**: stays `status: "draft"`; `upstream.url` is now the confirmed real value `https://github.com/mayswind/ezbookkeeping`; the invented `links` visibility (`"private-noted"`) and label (`"Repository (private)"`) were removed (`links: []` — the public/private decision is genuinely unconfirmed, so neither value is asserted); `contribution` and `upstream.relationship` are now `[CONTENT REQUIRED` markers instead of an authored claim.
- **Net effect on the public site**: `/work` now shows exactly one published entry (Professional Systems, under a "Featured systems" tier heading) instead of three. This is the correct, honest outcome per the remediation instruction's own rule ("do not preserve the desired order by inventing facts... it is acceptable for `/work` to show fewer published entries temporarily").
- **11-section case-study template built**: `components/project/{CaseStudyHero,LayerSection,DecisionList,NextProject}.tsx`, `components/ui/{Figure,Note,DecisionCallout}.tsx`, and 4 labelled placeholder SVGs (`public/images/projects/{kivilcim,dropspot,jointledger,professional}/placeholder-asset-pending.svg`). `app/work/[slug]/page.tsx` now composes all 11 PROJECT_SPEC §7 sections: hero (`CaseStudyHero`); one-minute summary/why it exists/constraints/evolution/reflection (authored as `##` headings in `index.mdx`, rendered as part of its compiled body — CONTENT_MODEL §3 marks these as index.mdx's own sections, needing no separate component); surface/flow/system (`LayerSection` ×3); decisions (`DecisionList`, from frontmatter); next project (`NextProject`). None of the current seed content is full/short depth, so sections 2–11 don't render on any live page today — proven structurally instead via a fixture (`tests/fixtures/content/work/delta-full`, extended with real headings, a `nextSlug`, and live `<Figure>`/`<Note>`/`<DecisionCallout>` usage inside its layer files) and a dedicated Vitest test (`tests/unit/work-loader.test.tsx`) that renders the real components via `renderToStaticMarkup` and asserts all 11 sections appear. This satisfies "renders... in dev" via the exact method this remediation instruction itself specified, without publishing invented project content.
- **MDX component whitelist populated**: `Figure`, `Note`, `DecisionCallout` — exactly the three named in ARCHITECTURE §6, no others. `blockJS`/`blockDangerousJS` remain explicit `true`.
- **Required-section heading gate implemented**: `checkRequiredSectionHeadings` in `lib/content/validate.ts`, applied per depth (full: one-minute summary/why it exists/constraints/evolution/reflection; short: one-minute summary/why it exists/reflection; preview/none: nothing required, matching CONTENT_MODEL §3's "description field only"). Identifies project, file, missing heading, and depth. 8 new unit tests (positive + negative for both full and short depth, plus the preview/none no-op cases).
- **`/work` is now tiered**: `getProjectsByTier` drives four groups in PROJECT_SPEC §9 order (Featured systems / Built for real life / Selected archive / Origins), each rendered only when non-empty — currently only "Featured systems" (1 entry) shows.
- **Gate demonstration, redone on real content**: temporarily bumped the real, published `professional-systems/index.mdx` to `depth: "short"` with a body missing the required "Reflection" heading (plus temporary valid layer files, to isolate the heading gate from the layer-existence/layer-meaning gates). `pnpm build` failed with exactly `Error: professional-systems (index.mdx): required section heading "Reflection" is missing (depth: short)` — a single, cleanly isolated failure. Reverted `index.mdx` (diffed byte-identical against a pre-edit backup) and deleted the three temporary layer files; `pnpm build` re-run clean.
- **Publication-model conflict closed**: confirmed Model B (`status: draft|published`; `depth: none|preview|short|full`, with "preview" a depth value, not a third status) is what CONTENT_MODEL §1 already specifies and what `lib/content/schemas.ts` already implements — no schema change was needed for this, only the documentation-level confusion in an earlier instruction's phrasing. No DECISIONS.md entry added for this specifically, since CONTENT_MODEL already correctly documents it.
- Full verification suite re-run clean after all changes: `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm build && pnpm exec playwright test`. Vitest: 76/76. Playwright: 58/58 on Chromium + WebKit.
- No new dependencies. No Client Components added. No commit made.
- Status: **STOPPED — TASK-004 remediated and re-verified. Awaiting review.**

### 2026-07-29 — Provisional development-placeholder metadata applied (Kıvılcım, DropSpot, JointLedger)

- **These are explicitly provisional development placeholders, not user-verified portfolio facts** — recorded here as instructed, so this is never mistaken for confirmed content later. Applied to unblock further implementation work without waiting on Hakan's input.
- Kıvılcım: `phase: "active-development"`, `aiAssisted: true` (+ placeholder `aiDisclosure`), `contribution: "I conceived, designed, and am developing Kıvılcım as a local-first personal planning, focus, and growth system."`. `status` stays `draft`, `depth` stays `preview`.
- DropSpot: `phase: "paused"`, `aiAssisted: true` (+ placeholder `aiDisclosure`), `contribution: "I designed DropSpot's core user flows and developed its frontend and backend foundation."`. `status` stays `draft`, `depth` stays `preview`. No concurrency/race-condition/scale/performance/user/outcome/production claim was added or restored.
- JointLedger: `phase: "active-development"`, `aiAssisted: true` (+ placeholder `aiDisclosure`), `contribution: "I extended ezBookkeeping with shared books, membership roles, invitation foundations, and BookId migration and backfill support."`. `upstream.url`/`upstream.name` confirmed (not placeholders, see above). No link or visibility value added for JointLedger's own repository — `links` stays empty. `status` stays `draft`, `depth` stays `preview`.
- Professional Systems: unchanged — `status: "published"`, `depth: "preview"`, `phase`/`aiAssisted` omitted, exact D-009 wording.
- `aiDisclosure` note: the schema (preserved as-is, no new enum or rule added) requires `aiDisclosure` whenever `aiAssisted` is `true`. Since the instruction specified `aiAssisted: true` without an aiDisclosure value, a `[CONTENT REQUIRED: AI-assistance disclosure details — pending confirmation]` marker was used — the same marker convention already used elsewhere for unconfirmed fields, not an invented disclosure statement.
- Body MDX text was left unchanged for all three drafts — only frontmatter metadata was in scope for this change. (Kıvılcım's and DropSpot's bodies still list "role statement"/"Hakan's specific contribution" among pending items, which is now slightly stale since `contribution` has a placeholder value; noted here rather than silently edited, since body copy wasn't part of this instruction's scope.)
- Verified empirically: all three draft projects 404 on direct access and are absent from `/work`'s rendered HTML; none of the new placeholder values (`active-development`, `paused`, AI-assistance text, the new contribution strings) appear anywhere in `/work` or `/work/professional-systems`'s rendered HTML. Professional Systems remains the only entry in `generateStaticParams`/the production build output.
- No new schema enum added. No new DECISIONS.md entry added (none was needed — D-018's existing optional-field behavior already covers this; nothing architectural changed). The TASK-004 acceptance criterion for the four-item D-016 public order remains unchecked, unmodified from the prior remediation pass.
- Full verification suite re-run clean: `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm build && pnpm exec playwright test`. Vitest: 76/76. Playwright: 58/58 on Chromium + WebKit.
- No new dependencies. No commit made.
- Status: **STOPPED — provisional metadata applied and verified. Still awaiting Hakan's real-fact confirmation before any of the three can publish.**

### 2026-07-29 — TASK-004 checkpoint commit

- The final strict review technically approved the TASK-004 implementation (infrastructure: schemas, gates, loaders, MDX pipeline, 11-section template, tiered `/work`, seed content). Committed and pushed as `bd6dd3b` ("feat: establish content system infrastructure") — explicitly an infrastructure checkpoint, not a TASK-004 completion commit. TASK-004 remains **PARTIALLY COMPLETE — BLOCKED ON USER-VERIFIED CONTENT**: criteria 3 and 4 (the four-item public order; zero-unverified-claims across three published previews) stay unchecked in `docs/tasks/TASK-004-content-system.md`, unchanged.
- Status: **Checkpoint pushed to `origin/main`. TASK-005 and later tasks not started.**

### 2026-07-31 — TASK-003 Homepage static implemented

- Built the complete static homepage on the real TASK-004 loaders — no disposable data layer (D-011 rejected). Created `components/sections/{Hero,PositioningStatement,LayerExplorerIntro,SelectedSystems,BuiltForRealLife,HowIBuild,FieldNotes,AboutPreview}.tsx` and `tests/e2e/home.spec.ts`; changed `app/page.tsx` (composes the 8 sections, reads `getProjectsByTier("featured"|"real-life")` and `data/notes.ts`, passes typed props down — no project data hard-coded anywhere) and `data/copy.ts` (new homepage chrome copy, see below).
- **Copy sourcing**: hero name/title/primary line (`homeWordmark`/`homePositioning`/new `heroPrimaryLine`) and the positioning statement (new `positioningStatement`) are exact copy from CLAUDE.md §4 / PROJECT_SPEC §11 — no refinement needed. The Surface/Flow/System one-liners (new `layerDefinitions`) are the exact wording from this task's own instruction. "Selected systems" and "Built for real life" headings/subheadings (new `selectedSystemsSubheading`/`builtForRealLifeSubheading`) are the exact PROJECT_SPEC §11 seed. **"How I build"'s four principles (new `howIBuildPrinciples`) have no literal pre-approved seed anywhere** — authored this task, each traced to an already-approved statement rather than invented: "Interfaces are built on systems" ← PROJECT_SPEC §1 purpose ("show both the visible interface and the underlying engineering decisions"); "Constraints are material" ← PROJECT_SPEC §2 System layer definition (permissions, offline strategy, data model); "Honest by default" ← DESIGN_SYSTEM §1.6 verbatim principle name; "Every element has a job" ← PROJECT_SPEC §14 acceptance criterion ("every decorative element has a role"). None of these are professional history, metrics, or claims requiring verification (CLAUDE.md §2) — they're voice/philosophy copy synthesized from already-approved concepts.
- **Selected systems / Built for real life**: both sections take a `projects` prop from `app/page.tsx`'s `getProjectsByTier` calls, reusing the existing `ProjectCard`. Today `getProjectsByTier("featured")` returns exactly one entry (Professional Systems — Kıvılcım/DropSpot/JointLedger remain draft, TASK-004 checkpoint, unchanged here) and `getProjectsByTier("real-life")` returns zero (no project currently carries that tier). No fake, disabled, or "coming soon" cards were created for the three drafts, per this task's explicit instruction; "Built for real life" instead shows new honest pending copy (`pendingCopy.realLife`) in the same D-009-adjacent tone already used for Lab/Notes/Work.
- **Images**: `professional-systems` has `images: []` in its real frontmatter, so zero images render on the homepage. DESIGN_SYSTEM §15's "image mat 7 cols / text 5 cols" alternating-row treatment for Selected Systems was not built — there is no real image to place, and adding placeholder imagery just to fill that layout would violate this task's explicit "no large decorative assets solely to fill space" instruction. The existing text-only `ProjectCard` is reused instead; revisit once real case-study imagery exists (TASK-005/006).
- **Accessibility**: the `h1` stays exactly `"Hakan Duyar"` (unchanged from the TASK-002 stub) — title and primary line are sibling paragraphs, not nested in the `h1` — so `tests/e2e/smoke.spec.ts` needed no change. Heading hierarchy is h1 → h2 (section headings) → h3 (Surface/Flow/System sub-blocks, How I build principle titles), no skips. All decorative grid vocabulary (hero index, layer registration mark) is `aria-hidden`, using only DESIGN_SYSTEM §8's approved vocabulary — nothing invented.
- **No new Client Components, no motion, no new dependencies** (`git diff --stat` on `package.json`/`pnpm-lock.yaml`: empty, confirmed).
- Full verification suite: `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm build && pnpm exec playwright test` — all green. Vitest: 76/76 (unchanged — no new unit-testable logic). Playwright: **92/92** on Chromium + WebKit (58 existing + 34 new `tests/e2e/home.spec.ts` tests: hero copy, one `h1`, positioning statement, static layer explanation, published-only listing, draft absence, no marker/metadata leaks, real-life pending state, field notes + about preview links, footer CTA, no-JS usability, no horizontal overflow at 375/768/1024/1440px).
- Manual fresh-production-server check (separate scratch port): `/` and `/work` both 200; header/footer render; homepage HTML contains no draft project names, no `CONTENT REQUIRED` marker, no provisional metadata (`active-development`, `ai-assisted`, etc.); exact hero/positioning/professional-systems copy present verbatim; exactly one `/work/` link (`professional-systems`); all four nav routes present in raw server HTML (no-JS reachable).
- **Deviations**: the D-016 four-item Selected Systems order and the placeholder-image criterion are honestly left unchecked (see `docs/tasks/TASK-003-homepage-static.md`) — same pattern as TASK-004's own unmet criteria, not reworded or weakened.
- No new dependencies. No commit made. TASK-005 and later tasks not started.
- Status: **STOPPED — TASK-003 implemented and verified. Awaiting static-layout approval (gates Phase 7 motion work).**

### 2026-07-31 — TASK-003 visual remediation (final review findings)

- The final strict review's screenshot-based visual inspection (not discoverable from code or automated tests alone) found two issues, both fixed: **the "Built in Layers" decorative registration mark** (three stacked bars) **visually read as a hamburger/menu icon** and was removed outright from `components/sections/LayerExplorerIntro.tsx` (no replacement glyph — the section now relies on typography, grid, and rules only, per the remediation instruction); and **the "About" section (`components/sections/AboutPreview.tsx`) had no visible heading**, unlike every other homepage section — a `<h2>About</h2>` was added, matching the same `display-l uppercase` pattern already used by Selected Systems / Built for Real Life / How I Build / Field Notes.
- `tests/e2e/home.spec.ts` updated: added a test confirming the About section has a visible `h2` named "About"; added a test confirming no `role="presentation"` element remains inside the Built in Layers section (a semantic, non-class-based check for the removed mark); added "About" to the existing no-JavaScript heading-visibility list.
- Full verification suite re-run clean: `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm build && pnpm exec playwright test`. Vitest: 76/76 (unchanged). Playwright: **96/96** on Chromium + WebKit (92 prior + 4 new).
- Fresh-production-server screenshots at 375×812/768×1024/1024×900/1440×1000 (captured outside the repo, deleted after inspection) confirmed: no hamburger-like mark at any width, "About" heading present with the same rhythm as surrounding sections, section indexes still 02–08 with no duplicate/gap, no new spacing imbalance, no horizontal overflow, no draft-content leaks, Professional Systems still the only public project, header/footer unchanged.
- TASK-003 acceptance criteria and their checked/unchecked states are unchanged by this remediation — no criterion text or status was touched. `docs/DECISIONS.md` untouched (no architectural decision involved). TASK-004 publication states unchanged; TASK-005 and later tasks not started.
- No commit made.
- Status: **STOPPED — both blocking visual-review findings remediated and re-verified. Awaiting approval.**

### 2026-07-31 — TASK-005 Kıvılcım case study: repository audit + full case-study text

- **Repository audit** (`https://github.com/hakanduyar/spark`, read-only, cloned to an isolated scratch directory outside this repo and deleted immediately after): confirmed real, extensive implementation — 14-table Dexie/IndexedDB schema across 8 versioned migrations, a genuine focus-timer state machine (idle/running/paused/done/completed, 5/15/25/45min presets), an idempotent `sourceKey`-guarded XP/level system, a real Vite PWA (Workbox service worker, installable manifest), transactional JSON export/import covering all 14 tables, and an optional Gemini AI layer gated behind a user-supplied `localStorage`-only key (grep-confirmed: never in source, never in backups). No secrets found in the repository; `.env*` properly gitignored.
- **Claims rejected by the audit, not published**: "Recharts" — absent from dependencies, explicitly listed as a future to-do item in the repo's own task log, not current fact. "Habit tracking" as a working feature — the `habits`/`habitLogs` tables exist in the schema and JSON backup but have zero UI anywhere; the repo's own decision log explains recurring commitments are instead handled by a separate `Routines` feature. "Weekly review" — schema-only, never wired to a screen outside the generic backup loop. All three corrected in `docs/CONTENT_INVENTORY.md`'s Kıvılcım section.
- **Content written**: `content/work/kivilcim/{index,surface,flow,system}.mdx` — full `depth: "full"` case study, all 11 IA sections, 6 real decisions (one, "Routines instead of Habits", sourced directly from the audited repository's own decision log), an honest AI disclosure (per this task's exact specified meaning, refined only for grammar), and a contribution statement implementing the exact boundary specified this turn (idea/scope/rules/IA/constraints/direction). `nextSlug` deliberately **omitted** — the task file's own implementation step assumed DropSpot would already be published by this point; since it's still `status: "draft"`, setting `nextSlug: "dropspot"` would have leaked a draft project's title into Kıvılcım's public page and linked to a route that 404s. Flagged as a deliberate deviation from that stale assumption.
- **Publication status**: kept `status: "draft"`, `verificationStatus: "partial"` (not `"verified"`) — CONTENT_MODEL §9 is explicit that only Hakan can move an item to `verified`, and that requires review of the *written text*, which this report requests rather than presumes. `factsCheckedAgainstRepo: true` is honestly set (the repository audit genuinely happened); `verificationStatus` is not.
- **Gate-readiness proven without live-publishing**: new `tests/unit/kivilcim-content.test.tsx` (14 tests) validates the real content directly — schema parse, required-heading gate, layer-meaning gate (length + pairwise similarity), and a full hypothetical `validatePublicationGates` pass (constructing a `published`/`verified` copy in memory, never touching the real file) — plus a `getProjectLayers("kivilcim")` call proving genuine, distinct, ordered Surface/Flow/System rendering (this function doesn't gate on publish status, so it works safely against draft content).
- **Temporary local publish+revert QA pass** (not committed, not left in place): temporarily edited `index.mdx`'s `status`/`verificationStatus` to `published`/`verified` only, ran a real `pnpm build` (confirmed `/work/kivilcim` appeared in `generateStaticParams`), took full-page screenshots at 375/768/1024/1440px, and ran a temporary Playwright spec (`zzz-tmp-kivilcim-qa.spec.ts`, deleted after) against a scratch server — **12/12 passed on Chromium + WebKit**, zero horizontal overflow at all 4 widths, honest placeholder alt text confirmed. Reverted `index.mdx` to its exact prior draft state, verified **byte-identical** via `diff` against a pre-edit backup, before any further work.
- **Real finding from the visual QA pass, not fixed (out of scope)**: the `##` headings inside `index.mdx`'s compiled body (One-minute summary, Why it exists, Constraints, Evolution, Reflection) render with **zero CSS styling** — `app/work/[slug]/page.tsx`'s wrapping div has no MDX component overrides or prose styling for bare `h2`/`h3`/`p`, so they're visually indistinguishable from body text (confirmed via raw HTML: `<h2>One-minute summary</h2>`, no classes). This is a pre-existing template gap from TASK-004, invisible until now because Kıvılcım is the first real `depth: "full"` content ever written. Not fixed here — TASK-005 is explicitly content-only ("if the template can't express something, STOP and report — that's a design decision"). Flagged for a future task.
- Full verification suite (final, real draft state): `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm build && pnpm exec playwright test` — all green. Vitest: **90/90** (76 prior + 14 new). Playwright: **96/96** on Chromium + WebKit (unchanged count — no new permanent E2E file added, since the existing draft-404 assertions for Kıvılcım already remain accurate).
- Manual fresh-production-server check (real draft state): `/`, `/work`, `/work/professional-systems` all 200; `/work/kivilcim`, `/work/dropspot`, `/work/jointledger`, unknown slug all 404; zero draft-name/marker/provisional-metadata/API-key/local-path leaks anywhere; Professional Systems byte-unchanged; zero horizontal overflow at 375/768/1024/1440px on `/` and `/work`.
- No code, component, or dependency changes. No new Client Components. TASK-006 and later tasks not started.
- No commit made.
- Status: **STOPPED — TASK-005 repository audit and case-study text complete. Kept as draft pending Hakan's review of the written text. Awaiting review.**

### 2026-07-31 — Shared MDX intrinsic-element typography gap repaired

- Fixed the shared TASK-004 case-study-template defect found in the TASK-005 final review: `##`/`###` headings, paragraphs, lists, emphasis, and links compiled from `index.mdx` bodies rendered as bare, unstyled HTML (e.g. `<h2>One-minute summary</h2>`, no classes) because `lib/content/mdx.ts`'s component whitelist only mapped the three custom components (`Figure`/`Note`/`DecisionCallout`), never the intrinsic markdown elements ARCHITECTURE §6 already allows.
- Renamed `lib/content/mdx.ts` → `lib/content/mdx.tsx` (needed JSX) and added controlled mappings for `h2`, `h3`, `p`, `ul`, `ol`, `li`, `strong`, `a` — each styled with an existing DESIGN_SYSTEM §3 role verbatim (h2→`heading-l`, h3→`heading-m`, p→`body`), no new token, no typography plugin, no global prose CSS, no `dangerouslySetInnerHTML`. `a` mirrors `TextLink`'s established underline/hover treatment without inventing external-link (target/rel) behavior. `blockJS`/`blockDangerousJS` remain explicit `true`; the custom-component whitelist is still exactly `Figure`/`Note`/`DecisionCallout` — no new custom component name was added.
- Caught and fixed a real sub-bug during visual QA: the first list styling attempt (`ml-5` margin on `<ul>`/`<ol>`) produced invisible bullets/numbers — Tailwind's preflight zeroes list padding, and `list-style-position: outside` markers need `padding-left` (not `margin-left`) to have room to render. Fixed to `pl-5 list-outside`, confirmed visually.
- New `tests/unit/mdx-components.test.tsx` (13 tests): proves each mapped element via real `compileProjectMDX` + `renderToStaticMarkup` output (not source-string search) — heading roles, list semantics, link semantics with no invented target/rel, `strong` weight, and that `Figure`/`Note`/`DecisionCallout` still render unaffected. Two tests directly re-prove the security posture against the new mapping: `blockJS`/`blockDangerousJS` still neutralize a JS expression (compiles but never evaluates it), and an arbitrary non-whitelisted component name still fails to render. `tests/unit/kivilcim-content.test.tsx` extended to assert the real Kıvılcım headings render with the actual `text-heading-l` class through the production mapping, not just as text.
- Temporary local publish+screenshot QA (not committed): flipped `content/work/kivilcim/index.mdx` to `published`/`verified` locally only, rebuilt, and visually confirmed at 375×812/768×1024/1024×900/1440×1000 that all five narrative headings now read clearly as headings, the Constraints list shows visible bullets, Figure/Note/DecisionCallout styling is unaffected, and there's zero horizontal overflow at any width. Reverted `index.mdx` to draft afterward, verified **byte-identical** via `diff` against a pre-edit backup; all temporary screenshots and scripts deleted.
- Full verification suite: `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm build && pnpm exec playwright test`. Vitest: **103/103** (90 prior + 13 new). Playwright: **96/96** on Chromium + WebKit (unchanged — no new E2E file, matching the established reasoning that a permanent test against a route that 404s in the final draft state would be dishonest).
- Kıvılcım remains `status: "draft"`; not published. DropSpot, JointLedger, and Professional Systems untouched. `docs/DECISIONS.md` untouched (no architectural decision — this is a bug fix inside an already-approved mechanism, ARCHITECTURE §6's restricted component map). TASK-003/004/005 acceptance criteria untouched.
- No commit made. TASK-006 and later tasks not started.
- Status: **STOPPED — shared MDX typography defect repaired and verified. Kıvılcım still draft, awaiting Hakan's review of the written text.**

### 2026-08-03 — Kıvılcım text approved (delegated editorial review); four accuracy corrections applied; checkpoint committed

- **Hakan explicitly delegated the line-by-line editorial review of the Kıvılcım case-study text to the assistant, and authorized the project to proceed based on the assistant's approval.** This is recorded plainly so it is never mistaken for Hakan having personally read every sentence: he did not; he delegated that review and accepted the assistant's judgment as the basis for proceeding.
- The assistant approved the complete Kıvılcım public-facing text (description, One-minute summary, Why it exists, Constraints, Surface, Flow, System, all six Decisions, Evolution, Reflection, Figure alt/caption) after the strict technical, content, and visual review already on record (see the two review entries above), subject only to four accuracy corrections:
  - **A — One-minute summary**: replaced the absolute "all data lives in IndexedDB... no required network connection" claim with a precise split — primary data in IndexedDB, small device-local configuration (including the optional Gemini key) in `localStorage`, no backend/account system, core workflows continue offline once installed.
  - **B — External network requests** (`system.mdx`): replaced "the only network calls the app ever makes are..." with "The application's only optional external data requests are user-authorized calls to Google's Gemini API" — removes an implicit claim about browser-level requests, service-worker updates, or static asset delivery that was never actually verified.
  - **C — Migration wording**: every instance of "without data loss" / "without losing user data" / "without ever discarding existing user data" (three occurrences: the `kivilcim-d2` decision, the Reflection section, and `system.mdx`) replaced with "eight versioned schema migrations designed to preserve existing records as tables and indexes changed" — describes the implementation's intent, not an unverifiable historical guarantee that no user, ever, lost data.
  - **D — Contribution statement**: trimmed to the exact approved sentence, removing the trailing "— see the AI disclosure below..." clause. The AI disclosure itself required no change — it already matched the approved wording exactly.
- No other content was rewritten. All other sections, all six Decisions' titles/context/alternatives/trade-offs, both layer summaries, and the Figure alt text/caption are byte-identical to the text reviewed and reported on previously.
- `docs/CONTENT_INVENTORY.md` was **not** modified this turn — none of the four corrections required an existing CONTENT_INVENTORY wording entry to stay consistent (that file's Kıvılcım section is a higher-level audit summary, not a copy of the case-study prose), and the instruction was explicit not to broaden it unnecessarily.
- `docs/tasks/TASK-005-kivilcim-case-study.md`: criteria 5 (contribution) and 8 (Hakan's approval) moved to `[x]`, on the basis of the explicit delegation above, not personal line-by-line reading. Criterion 6 (real images) stays `[ ]` — real screenshots remain the task's sole outstanding blocker. Original wording preserved verbatim on all eight criteria; only `[x]`/`[ ]` used. Task status set to **PARTIALLY COMPLETE — BLOCKED ON REAL PROJECT ASSETS** — not marked fully complete.
- `content/work/kivilcim/index.mdx` and `system.mdx` are the only content files touched this turn (`surface.mdx` and `flow.mdx` needed no correction). `status: "draft"` and `verificationStatus: "partial"` are unchanged — Kıvılcım is not published; real screenshots are still required before that can change.
- Full verification suite re-run clean: `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm build && pnpm exec playwright test`. Vitest: **103/103** (unchanged — the corrections didn't require new or modified tests; all existing assertions, including the real-content semantic-rendering test, still pass against the corrected wording). Playwright: **96/96** on Chromium + WebKit.
- Temporary local publish+revert QA (not committed): flipped `index.mdx` to `published`/`verified` locally only to confirm the corrected text renders correctly and Kıvılcım is genuinely absent from the public homepage/`/work` while draft; reverted byte-identical afterward (`diff`-confirmed), all temporary artifacts deleted.
- Checkpoint commit created and pushed to `origin/main` (`feat: add audited Kıvılcım case study draft`) — explicitly a draft checkpoint, not a TASK-005 completion commit. Kıvılcım remains unpublished.
- Status: **STOPPED — Kıvılcım text approved via delegated review, checkpoint pushed. TASK-005 remains partially complete, blocked on real screenshots. TASK-006 and later tasks not started.**

### 2026-08-05 — D-019 provisional-asset policy; Kıvılcım published with verified/provisional diagrams; TASK-005 complete

- **New binding decision (D-019, `docs/DECISIONS.md`)**: Hakan explicitly changed the prior "real screenshots only" asset rule. Until real screenshots/photographs/video exist, a project may instead use repository-verified `verified-diagram`/`provisional-illustration` assets, provided every asset is visibly labelled as illustrative/diagrammatic, never styled or captioned closely enough to be mistaken for a real screenshot, and traces only to repository-verified facts — never an invented UI, metric, user, client, outcome, or feature. Real assets replace provisional ones later as a pure swap; no other truthfulness rule is relaxed.
- **Schema/content-model change**: `lib/content/schemas.ts`'s `ProjectImageSchema.placeholder: boolean` replaced with `assetType: "real-screenshot" | "verified-diagram" | "provisional-illustration"` (new `ProjectImageAssetTypeSchema` export) — a single, non-duplicated field rather than a second flag alongside the old one. `docs/CONTENT_MODEL.md` §2/§6 updated to match exactly; §4's layer-figure guidance updated to name the D-019 fallback. `lib/content/validate.ts` had one stale comment updated (no gate logic changed — image honesty is verified manually per task, same as before).
- **Four new SVG assets created** for Kıvılcım, each built only from facts already established in the TASK-005 repository audit, with an accessible `<title>`/`<desc>` pair, a visible required label, and no scripts/`foreignObject`/external resources/embedded raster:
  - `product-areas-map.svg` — the five product areas (Today, Plan, Mind, Focus, Progress) as labelled panels, not a fake screen. Label: "ILLUSTRATIVE PRODUCT MAP — NOT A SCREENSHOT". `assetType: provisional-illustration`.
  - `core-flow-diagram.svg` — Capture → Plan → Focus → Review with a return loop. Label: "VERIFIED FLOW DIAGRAM". `assetType: verified-diagram`.
  - `local-first-architecture.svg` — Interface/Dexie/IndexedDB/PWA-Workbox/JSON export-import inside an on-device boundary, with the optional Gemini request path shown clearly outside the core boundary. Label: "VERIFIED ARCHITECTURE DIAGRAM". `assetType: verified-diagram`.
  - `focus-lifecycle.svg` — Idle → Running → Paused → Done → Completed, with the verified reset-to-Idle-without-partial-credit transition. Label: "VERIFIED STATE DIAGRAM". `assetType: verified-diagram`.
- **Integration**: all four registered in `index.mdx`'s `images[]` array (src/alt/caption/layer/assetType) and rendered via `<Figure>` in `surface.mdx` (product-areas-map), `flow.mdx` (core-flow-diagram, focus-lifecycle), and `system.mdx` (local-first-architecture), each with honest alt text and a caption stating plainly it is an illustration/diagram, never a screenshot. The old generic `placeholder-asset-pending.svg` (from TASK-004) was removed from `surface.mdx` and deleted from disk — it is no longer referenced anywhere in Kıvılcım.
- **Acceptance-criteria reconciliation, not silent rewrites**: `docs/tasks/TASK-005-kivilcim-case-study.md` criterion 6 ("all images real... no placeholder assets remain") — original wording preserved, a superseded-requirement note added naming D-019 and the date, and the current approved criterion (real screenshot OR labelled verified-diagram/provisional-illustration) recorded as met. Criterion 1 and criterion 6 (code/dependency scope) updated with factual notes about what actually changed this turn (real publish vs. hypothetical; the narrow, D-019-authorized schema edit). `docs/tasks/TASK-003-homepage-static.md`'s "every image is a labelled placeholder SVG" criterion received the same superseded-requirement treatment — still honestly left unchecked, since the homepage itself renders zero project images regardless (confirmed by inspection: no `Figure`/`images[]` usage in `app/`/`components/sections/`). `docs/tasks/TASK-004-content-system.md`'s two unmet four-project-order criteria were **not** touched — only Kıvılcım and Professional Systems are published; DropSpot and JointLedger remain draft, so that criterion genuinely still fails and was left exactly as it was.
- **Publication**: independently re-verified all TASK-005 publication conditions against the live content (not a hypothetical copy) — `validatePublicationGates` passes with zero errors (schema, marker, verification/depth, required-section-heading, and layer-meaning gates, the last unaffected by the new `<Figure>` insertions per a direct re-run of `checkLayerMeaning`). `content/work/kivilcim/index.mdx` flipped for real: `status: "published"`, `verificationStatus: "verified"`; `factsCheckedAgainstRepo: true`, `aiAssisted: true`, the approved AI disclosure, the approved contribution statement, `depth: "full"`, `order: 1` all unchanged. `nextSlug` remains omitted (DropSpot is still draft).
- **Public ordering**: via the existing loader (`getProjectsByTier`/`sortByTierThenOrder`, no hard-coding) — Kıvılcım (`order: 1`) now appears before Professional Systems (`order: 4`) on both `/` and `/work`; DropSpot (`order: 2`) and JointLedger (`order: 3`) remain draft and absent, confirmed by a fresh production build's `generateStaticParams` output and by direct HTML inspection.
- **Tests added/updated**: new `tests/unit/kivilcim-assets.test.ts` (50 tests) — every asset exists, parses as valid SVG, has `role="img"` with a real `<title>`/`<desc>` pair (`aria-labelledby` ids resolve), visibly renders its required label as SVG text, contains no `<script>`/`<foreignObject>`/embedded raster/external resource reference, never claims to be a real screenshot outside an honest denial, and contains no credential-shaped string or local path; plus frontmatter-level checks that all four `images[]` entries point at real files with a valid `assetType`, non-empty alt, and honest caption, and that the old placeholder is registered nowhere. `tests/unit/kivilcim-content.test.tsx` updated: now asserts `published`/`verified` directly (no longer a hypothetical override), and the surface-Figure test checks the new `product-areas-map.svg` asset instead of the deleted placeholder. `tests/unit/mdx-components.test.tsx`'s generic Figure test decoupled from the now-deleted kivilcim placeholder path. New Playwright coverage in `tests/e2e/work.spec.ts` ("Work: Kıvılcım case study") and updates to `tests/e2e/home.spec.ts`/`tests/e2e/work.spec.ts`'s listing tests: `/work/kivilcim` 200 with all 11 IA sections and all four D-019 assets visible with honest captions, no generic placeholder, no `CONTENT REQUIRED` marker or credential-shaped string, no dangling Next-project link to the still-draft DropSpot, and no horizontal overflow at 375/768/1024/1440px.
- Full verification suite: `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && rm -rf .next && pnpm build && pnpm exec playwright test --list && pnpm exec playwright test && git diff --check` — all green. Vitest: **153/153** (103 prior + 50 new). Playwright: **118/118** on Chromium + WebKit (96 prior + 22 net new = 11 unique test cases × 2 browsers: `home.spec.ts` +1 named test ("Kıvılcım renders with its real title and description"), `work.spec.ts` +7 named tests, one of which is a 4-width loop expanding to 4 runtime instances, for +10 total in that file. No existing test was deleted — the three single-published-project assertions ("lists only the published project", "draft projects... do not appear" ×2) were narrowed and renamed in place to reflect that Kıvılcım is now published alongside Professional Systems, not retired. `git diff --check`: clean.
- Manual fresh-production-server check (real, now-published state): `/` → 200, `/work` → 200, `/work/kivilcim` → 200, `/work/professional-systems` → 200, `/work/dropspot` → 404, `/work/jointledger` → 404; all four SVGs served with `Content-Type: image/svg+xml`; Kıvılcım's link precedes Professional Systems' on both `/` and `/work`; zero `CONTENT REQUIRED` marker, zero local-path/API-key-value leak, zero remaining reference to the deleted placeholder anywhere in the served HTML.
- Visual review (temporary screenshots, captured outside the repo and deleted immediately after inspection) at 375×812/768×1024/1024×900/1440×1000, and per-figure crops at 375px/1440px: all four assets render legibly with their required label prominent, correct DESIGN_SYSTEM tokens (paper/ink/line/signal), no gradients/glassmorphism, no clipped content, no horizontal overflow at any width. Honest observation, not a blocker: at 375px the diagrams' body text is small (proportional scaling of a fixed 1600×1000 viewBox) — legible but not large; since the assets are vector, they stay crisp at any zoom, and every fact shown is also stated in the surrounding prose, so no information is exclusively locked behind small diagram text.
- No new dependency was added (`git diff --stat` on `package.json`/`pnpm-lock.yaml`: empty). No Client Component was added. No template/component/layout file was changed — only content, the one D-019-authorized schema field, docs, and tests.
- No commit made. TASK-006 and later tasks not started.
- Status: **STOPPED — D-019 adopted, Kıvılcım published under it with four honestly-labelled verified/provisional assets, TASK-005 complete. Awaiting review.**

### 2026-08-05 — TASK-006 DropSpot case study complete; reusable D-019 asset infrastructure added

- **Repository audit**: `https://github.com/hakanduyar/dropspot-project` (identified unambiguously from four independent project-controlled sources — ROADMAP.md, the TASK-006 task file, `docs/CONTENT_INVENTORY.md`, and DropSpot's own frontmatter `links[]`), cloned read-only (shallow, then unshallowed for full commit history) into an isolated scratch directory outside this repo, audited directly at the source level, then deleted and confirmed gone. Every technical claim published was checked against actual code, not the README alone: the claim/waitlist transaction logic (`dropspot-backend/src/models/Claim.js`, `Waitlist.js`) genuinely uses `BEGIN`/`SELECT ... FOR UPDATE`/`COMMIT` — real row-level locking, not just a documented intention; the priority-score seed formula (`seedGenerator.js`) matches its own unit tests exactly; the schema (`migrate.js`) has real `CHECK` constraints and a composite index; real Jest/Supertest and Vitest/RTL test suites exist. Two genuine limitations were found that the README's own framing doesn't surface: a GitHub Actions CI workflow is committed at `github/workflows/ci.yml` instead of `.github/workflows/ci.yml`, so it never actually ran; and the frontend's `signup_latency_ms` value is computed from two `Date.now()` calls issued back to back on the same line, so it's near-inert in the priority formula. Both are stated as limitations in the published text, not omitted. 8 real product screenshots and 1 database-schema diagram exist in the repository's own `screenshots/` directory — not imported this pass (irregular aspect ratios, no 16:10 match, would need cropping/optimization and a subject decision) — flagged as a concrete follow-up in `docs/CONTENT_INVENTORY.md` rather than attempted speculatively.
- **Content written**: `content/work/dropspot/{index,surface,flow,system}.mdx` — `depth: "short"` case study, One-minute summary/Why it exists/Constraints/Reflection (Evolution omitted — genuinely not much to say about a single 2-day build burst beyond what Reflection already covers), all three layers distinct (2136/2232/3914 stripped characters; pairwise similarity 26.6%/27.7%/29.4%, well under the 60% gate), System deliberately the deepest per this task's own direction. Four real decisions, each with genuine alternatives and a trade-off traced to code (row-locking vs. an application queue/optimistic concurrency; the deployment-unique seed vs. a fixed formula; the single recreate-schema script vs. versioned migrations; server-side claim-window re-checking vs. trusting the client).
- **Contribution and AI disclosure, an honest departure from the Kıvılcım pattern (not assumed equivalent)**: `contribution` is derived from git history — all 31 commits, across 2025-11-06–2025-11-07, authored solely by Hakan Duyar, no forks, no co-authors — under this task's own explicit instruction to derive it from verified history rather than wait for a live intake round-trip. `aiAssisted` is **omitted entirely**, not set to a guessed value either way — no repository evidence of AI assistance was found (no artifacts, no commit-message hints), and CLAUDE.md forbids inventing that fact; this follows D-018's existing precedent for exactly this situation rather than reusing Kıvılcım's `aiAssisted: true` boundary, which this project's evidence doesn't support.
- **D-019 assets**: four new SVGs under `public/images/projects/dropspot/` — `screens-map.svg` (`provisional-illustration`, DropSpot's 7 real routes grouped into 5 areas), `core-flow-diagram.svg` (`verified-diagram`, Browse→Join→Claim window→Claim with an honest-failure branch), `claim-transaction-diagram.svg` (`verified-diagram`, the full row-lock/check/commit sequence, quoting `Claim.js` directly), `priority-score-diagram.svg` (`verified-diagram`, the seed→coefficients→formula chain, visually flagging the near-inert latency term in `--signal-text`). Same visual language and security posture as Kıvılcım's four (role="img", title/desc, no scripts/foreignObject/external resources/raster/animation/event handlers, only approved DESIGN_SYSTEM tokens). The old generic placeholder was deleted.
- **Reusable D-019 infrastructure** (this task's own explicit "improve before the next project" instruction, not scope creep): `lib/content/validate.ts` gained `checkImageAssets(slug, images, fileExists?)` — path safety (project-relative, no `..` traversal), an extension allow-list per `assetType` (`ALLOWED_ASSET_EXTENSIONS`: `.svg` only for `verified-diagram`/`provisional-illustration`, raster for `real-screenshot`), on-disk existence (injectable predicate, defaults to real `fs.existsSync`), and a lexical honesty check that a `verified-diagram`/`provisional-illustration` caption actually says "diagram" or "illustrat…". Wired into `validatePublicationGates` for every project, not just DropSpot — re-ran Kıvılcım's existing tests afterward and confirmed its four assets still pass cleanly with zero errors. `docs/CONTENT_MODEL.md` §6 rewritten to document this gate and explicitly state what it cannot do (prove factual correctness — that stays a repository-audit and human-review responsibility); `docs/ARCHITECTURE.md` §8 corrected (it still described the retired `placeholder: true|false` field from before D-019); `docs/DESIGN_SYSTEM.md` §9 updated with the D-019 diagram-sizing convention (fixed 16:10 canvas, matching screenshots, not "free height" as originally written) and mobile-legibility expectations.
- **Publication**: `validatePublicationGates` returns zero errors against the live, published content. `content/work/dropspot/index.mdx`: `status: "published"`, `verificationStatus: "verified"`, `depth: "short"`, `order: 2`, `factsCheckedAgainstRepo: true`. `nextSlug: "professional-systems"` (published, non-404). Kıvılcım's own `nextSlug` updated to `"dropspot"` (previously omitted, since DropSpot was draft) — now valid since DropSpot is itself published.
- **Public ordering**: via the existing loader, no hard-coding — Kıvılcım (`order: 1`) → DropSpot (`order: 2`) → Professional Systems (`order: 4`) on both `/` and `/work`, confirmed via `generateStaticParams` output and direct HTML inspection. JointLedger (`order: 3`) remains draft and absent; no fake card renders for it.
- **Task-status impact**: TASK-006 marked COMPLETE, all 7 original acceptance criteria preserved verbatim with honest notes (two carrying an explicit caveat: the contribution/learning statement is evidence-derived, not personally written by Hakan, and one narrow D-019-authorized `lib/content/validate.ts` change occurred beyond the original file list). TASK-003 and TASK-004's D-016 four-project-order criteria remain correctly **unchecked** — updated the explanatory notes to reflect that 3 of 4 are now published (only JointLedger missing), not silently marked satisfied. TASK-005 untouched, remains COMPLETE.
- **Tests added**: `tests/unit/dropspot-assets.test.ts` (61 tests, same structural/accessibility/security pattern as Kıvılcım's asset tests, plus a real run of `checkImageAssets` against the actual files), `tests/unit/dropspot-content.test.tsx` (14 tests — schema, gates, layer distinctness, real component rendering, `nextSlug`), 13 new tests for `checkImageAssets` itself in `tests/unit/validate.test.ts` (path traversal, missing file, wrong extension, caption honesty, using injected fakes rather than the real filesystem). New Playwright coverage in `tests/e2e/work.spec.ts` ("Work: DropSpot case study") and updates to the existing listing/ordering tests in `work.spec.ts`/`home.spec.ts` to reflect 3 published projects instead of 2, plus a rewritten Kıvılcım "Next project" test (previously asserted the link was absent; DropSpot's publication makes that assertion false, so it now asserts the link is present and resolves to 200).
- Full verification suite: `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && rm -rf .next && pnpm build && pnpm exec playwright test --list && pnpm exec playwright test && git diff --check` — all green. Vitest: **243/243** (154 prior + 89 new: 61 dropspot-assets + 14 dropspot-content + 13 checkImageAssets + 1 Kıvılcım nextSlug fix). Playwright: **140/140** on Chromium + WebKit (118 prior + 22 net new — 11 unique cases × 2 browsers: a new "Work: DropSpot case study" block plus updated listing/ordering assertions). `git diff --check`: clean.
- Manual fresh-production-server check: `/` → 200, `/work` → 200, `/work/kivilcim` → 200, `/work/dropspot` → 200, `/work/professional-systems` → 200, `/work/jointledger` → 404, unknown slug → 404; all four new SVGs served with `Content-Type: image/svg+xml`; ordering on `/` and `/work` confirmed Kıvılcım → DropSpot → Professional Systems; zero `CONTENT REQUIRED` marker, zero credential/local-path leak, zero placeholder reference anywhere in served HTML.
- Visual review (temporary screenshots and per-figure crops, captured outside the repo and deleted after inspection) at 375×812/768×1024/1024×900/1440×1000: zero horizontal overflow on `/`, `/work`, `/work/dropspot` at every width; all four DropSpot assets render legibly and on-brand, same honest observation as Kıvılcım's about small-but-crisp mobile diagram text (every fact duplicated in surrounding prose).
- No new dependency was added (`git diff --stat` on `package.json`/`pnpm-lock.yaml`: empty). No Client Component was added. No template/component/layout file was changed.
- No commit made. TASK-007 and later tasks not started.
- Status: **STOPPED — TASK-006 complete, DropSpot published under D-019 with four honestly-labelled assets, reusable D-019 asset validation now shared across every project. Awaiting review.**

### 2026-08-05 — TASK-006 remediation: contribution wording fixed, real DropSpot screenshots integrated

- **Contribution wording**: `content/work/dropspot/index.mdx`'s `contribution` field changed from "...and implemented the Express/PostgreSQL backend and React frontend." — replacing an earlier draft ending "...built... myself", which overstated provenance beyond what git-history evidence (31/31 commits solely authored by Hakan Duyar) actually establishes. `aiAssisted` remains omitted (unchanged).
- **`Figure` component check before touching real screenshots**: confirmed `components/project/Figure.tsx` renders a plain `<img className="block w-full">` with no fixed `height`/`aspect-ratio` — it already preserves any image's true intrinsic aspect ratio with zero component changes needed. No edit made to `Figure.tsx` or any other component.
- **Repository re-audited a second time, read-only**: shallow-then-full clone of `https://github.com/hakanduyar/dropspot-project` into an isolated scratch directory outside this repo, deleted and confirmed deleted after. All 8 real screenshots + the repository's own "schema diagram" were viewed directly (not inferred from filenames or the README's own captions).
- **Two source-repository inaccuracies caught by direct inspection, not trusted from labels**: `waitlist-claimed.png` (README: "user successfully claimed a drop, shows unique claim code") actually shows the waitlist-**joined** confirmation state, not a claim — no claim code is visible. `my-claims.png` (README: "user's claimed drops with unique claim codes") actually shows the empty "No claims yet" state. No screenshot in the repository shows a completed claim with a visible code; the case study makes no such claim. `db.png` ("Database Schema (Bonus)... showing relationships between tables") is actually a pgAdmin dashboard of generic server activity graphs (sessions, transactions/sec, tuples, block I/O) — not a schema diagram at all, no table structure visible — rejected outright, not imported under any description.
- **4 of 8 real screenshots selected** for distinct, verified product value: `home-login.png` → `browse-drops.webp` (home, signed in, waitlist-status badges — chosen over the logged-out `home.png`, which shows strictly less state), `drop-detail.png` → `drop-detail.webp` (pre-join drop detail), `waitlist-claimed.png` → `waitlist-joined.webp` (post-join confirmation, renamed to match what it actually shows), `admin-panel.png` → `admin-panel.webp` (admin CRUD table). Not selected: `home.png` (redundant), `sign-up.png` (generic form, no DropSpot-specific value), `my-waitlist.png`/`my-claims.png` (low unique value; the latter is an empty state).
- **Safe, lossless optimization only**: system-installed Python/Pillow (not a new project dependency — verified via `git diff --stat` on `package.json`/`pnpm-lock.yaml`, empty) re-encoded all 4 selected PNGs as lossless WebP. Verified pixel-for-pixel identical via full `getdata()` comparison before/after (0 differing pixels, max channel diff 0, dimensions unchanged) for all four. No cropping applied — dimensions preserved exactly (1806×822, 1731×837, 1804×812, 1878×808). Sizes: 573KB→339KB, 806KB→497KB, 742KB→455KB, 102KB→31KB.
- **`screens-map.svg` (`provisional-illustration`) removed**: real screenshots now cover 3 of its 5 illustrated interface areas with genuine evidence; per D-019's own "real assets replace provisional ones... as a pure asset swap" principle, keeping a provisional stand-in once real evidence exists for its core purpose was judged inconsistent. File deleted from `public/images/projects/dropspot/`, its frontmatter entry and `<Figure>` in `surface.mdx` removed, its dedicated tests replaced.
- **Final asset composition (7 total, was 4)**: Surface — `browse-drops.webp`, `drop-detail.webp`, `admin-panel.webp` (3 real screenshots, 3 distinct interface areas). Flow — `waitlist-joined.webp` (1 real state screenshot) + `core-flow-diagram.svg`. System — `claim-transaction-diagram.svg`, `priority-score-diagram.svg` (unchanged). All real-screenshot entries carry `assetType: "real-screenshot"`, honest alt/caption describing only what is genuinely visible, no "not a screenshot" denial (correctly — they are screenshots). `checkImageAssets` (`lib/content/validate.ts`) already scoped its diagram/illustration honesty-caption check to `verified-diagram`/`provisional-illustration` only, so real screenshots were already correctly exempt — no code change was needed there.
- **Documentation updated for consistency**: `docs/DESIGN_SYSTEM.md` §9 — new bullet clarifying 16:10 is a preferred *authored*-screenshot target, not a requirement imposed on real evidence; real screenshots keep their true aspect ratio, `Figure` needs no change, cropping is allowed only to remove irrelevant outer chrome, never to change the depicted product state. `docs/CONTENT_MODEL.md` §4 — one clause added noting the 16:10 diagram convention does not extend to `real-screenshot` assets. `docs/CONTENT_INVENTORY.md` — DropSpot section rewritten with the two source-repository-inaccuracy findings, the 4-selected/4-not-selected reasoning, and the `db.png` rejection; "Missing visual assets" section updated to mark DropSpot's screenshot slot done and note it as the first project using real screenshots under D-019.
- **`docs/tasks/TASK-006-dropspot-case-study.md`**: status header appended with a "Remediation (same day)" note; criteria for layer distinctness (updated char counts: surface 2834/flow 2510/system 3914, similarity 31.1%/28.3%/30.7%), contribution (wording-fix explanation), diagrams (3 remain + 4 real screenshots added, `screens-map.svg` removal explained), and Hakan-approval (delegated authority exercised twice) all updated with honest notes; original acceptance-criteria text preserved.
- **Tests**: `tests/unit/dropspot-assets.test.ts` fully rewritten for the mixed 3-diagram/4-screenshot composition (SVG structural/security checks scoped to the 3 diagrams; new WebP-file/registry checks for the 4 screenshots, including a real `checkImageAssets` run against real files) — 54 tests (was 61 for the old 4-diagram set). `tests/unit/dropspot-content.test.tsx` updated: the "all Figures render" test now checks all 7 assets by their new layer placement, plus two new tests confirming real screenshots never carry the diagram-denial phrase and diagrams still do — 18 tests (was 14). `tests/e2e/work.spec.ts`'s DropSpot asset test rewritten for 7 images with their real captions; a new Playwright test added asserting each real screenshot's rendered bounding-box aspect ratio matches its true intrinsic ratio (within 2%) and is measurably not 16:10 — proving `Figure` doesn't stretch real evidence to the diagram convention.
- Full verification suite: `pnpm typecheck` clean, `pnpm lint` clean, `pnpm format:check` clean (after one Prettier auto-fix on the new asset test file), `pnpm test` **238/238**, `rm -rf .next && pnpm build` succeeded (routes unchanged: `/`, `/work`, `/work/kivilcim`, `/work/dropspot`, `/work/professional-systems`), `pnpm exec playwright test` **142/142** (Chromium + WebKit, up from 140 with the new aspect-ratio test on both browsers), `git diff --check` clean.
- Manual fresh-production-server checks (port 3105, torn down and confirmed stopped after): exact route-status matrix (`/`, `/work`, all 3 published slugs → 200; `/work/jointledger`, unknown slug → 404); all 4 WebP assets served as `image/webp`, all 3 SVGs as `image/svg+xml`; the removed `screens-map.svg` genuinely 404s; zero CONTENT REQUIRED/placeholder/credential/local-path/JointLedger-link leaks in the live HTML; Kıvılcım→DropSpot→Professional-Systems order confirmed on both `/` and `/work`.
- Visual review at all four required widths (375×812, 768×1024, 1024×900, 1440×1000) on `/`, `/work`, `/work/dropspot`: zero horizontal overflow anywhere. Each of the 7 figures individually captured and inspected at desktop width (clean mat/corner-tick styling, honest captions, no stray markup) and at 375px (all remain legible, including the two technical diagrams' smaller vector text — expected per DESIGN_SYSTEM §9, not a defect). All temporary screenshots, scripts, and the second scratch clone deleted and confirmed gone.
- No new dependency was added (`git diff --stat` on `package.json`/`pnpm-lock.yaml`: empty; Python/Pillow used was already system-installed, not added to the project). No Client Component was added. No template/component/layout file was changed (`Figure.tsx` confirmed to need no edit).
- No commit made. TASK-007 and later tasks not started.
- Status: **STOPPED — TASK-006 remediation complete. DropSpot now uses 4 real, repository-sourced screenshots plus 3 verified technical diagrams (7 assets total), with two source-repository mislabelling issues caught and corrected during re-audit. Contribution wording corrected to avoid overstating provenance. Awaiting review.**

### 2026-08-10 — JointLedger published (TASK-004 content-system/publication scope, not a new task)

- **Not a new numbered task**: this work completes TASK-004's own original "In scope" entry (`content/work/jointledger/index.mdx`, listed in that task's Exact-files table from the start) and the D-016 homepage order it was always meant to support — it does not become "TASK-006.5" or any other new roadmap item, per this pass's explicit governing instruction. `docs/ROADMAP.md`'s separate proposed "TASK-009 — JointLedger full case study" is a different, not-yet-approved, deeper future proposal; this pass published at `depth: "short"`, not "full", so it does not silently consume that proposal — left untouched, not edited this pass.
- **Repository audit, read-only, both repositories, both branches**: `https://github.com/hakanduyar/jointledger` — `main` (HEAD `2d1f9776`, effectively upstream history plus one setup commit) and the unmerged `feature/shared-family-book` (HEAD `3ab9cb0f`, 4 commits, all by Hakan Duyar, all on 2026-07-07 between 16:08 and 23:48) — plus upstream `https://github.com/mayswind/ezbookkeeping` (HEAD `09e13e9b`, MIT, MaysWind) for accurate attribution. Both clones deleted after the audit, deletion confirmed.
- **Verified implemented**: a real `Book`/`BookMember`/`BookInvitation` data model (`pkg/models/book.go`) with owner/editor/viewer roles and real permission-checking methods; `Account`, `TransactionCategory`, and `TransactionTag` genuinely book-scoped end-to-end (every API handler calls `resolveBookContext` → `CheckBookPermission` → a `WHERE book_id=?` query — confirmed directly in `pkg/api/{accounts,transaction_categories,transaction_tags}.go`); an idempotent personal-book backfill (`EnsurePersonalBook`, `BackfillAllPersonalBooks`, CLI `backfill-books`) inside a single DB transaction; last-owner-removal protection. All exercised by real tests reading real DB state (`TestBookScopedData_SharedBookAndIsolation`, `TestChangeBookMemberRole_OwnerAndLastOwnerRules`, etc.) — read directly, not executed (no Go toolchain in this environment; verification was direct source reading, disclosed honestly).
- **Verified NOT implemented, the most important finding**: `Transaction.BookId` exists and is set on creation/backfilled, but every transaction query in `pkg/services/transactions.go` still filters by `uid`, never `book_id` — zero `resolveBookContext` calls anywhere in `pkg/api/transactions.go`. Two members of a shared book cannot see each other's transactions through this system yet, the single most central promise of a "shared ledger." `BookInvitation` is schema/error-code-only — its table is synced and `ErrBookInvitationNotFound` is defined, but no service or API function anywhere creates, sends, lists, accepts, or declines one; membership today is direct add-by-owner. Zero frontend integration exists — a repository-wide search for any book-id identifier across the entire Vue source returns nothing.
- **A genuine repo-inconsistency finding, trusted-code-over-docs (same pattern as DropSpot's misplaced CI file)**: `README-JOINTLEDGER-LOCAL.md` (Hakan's own file, written 2026-07-03) still says "no custom features yet... has not been started" — it simply predates the feature branch by four days and was never updated. The case study trusts the audited code, not this stale file.
- **A mild security finding, disclosed architecturally, not as exploit instructions**: `BookMemberAddHandler` resolves a target user by email/username (`resolveTargetUser`) before the actor's own manage-permission on the target book is checked inside `AddBookMember`, which could let an authenticated caller probe whether an email belongs to a registered account independent of their own access to that book.
- **Final metadata**: `depth: "short"` (a "strong short" case study judged more honest than "full," given how much of the shared-ledger promise remains infrastructural), `verificationStatus: "verified"`, `factsCheckedAgainstRepo: true`, `status: "published"`, `provenance: "fork"`, `upstream: { name: "ezBookkeeping", url: "https://github.com/mayswind/ezbookkeeping", relationship: <full audited description> }`, `aiAssisted` omitted (no repository evidence either way, same D-018 pattern as DropSpot). Contribution statement explicitly states what Hakan did *not* write (ezBookkeeping's transaction engine, accounting logic, UI) alongside what he did.
- **Upstream disclosure was previously unrendered anywhere — a genuine, narrow component fix, not new product design**: `components/project/CaseStudyHero.tsx` and `ProjectCard.tsx` never rendered `project.upstream` at all before this pass, despite `docs/CONTENT_MODEL.md` §9 already mandating it ("mandatory in any rendering, including preview cards") and TASK-004's own acceptance criteria already requiring it. Both components now render a short, visible fork-disclosure line (with a real link to the upstream repository on the case-study hero) whenever `project.upstream` is set — applies to any future fork-provenance project, not JointLedger alone. This completes an already-approved requirement; it introduces no new architectural decision, so `docs/DECISIONS.md` was not touched.
- **4 D-019 `verified-diagram` SVGs created** (no real screenshots exist — there is no frontend UI to capture): `upstream-extension-map.svg` (Surface — what's inherited vs. added), `personal-book-backfill-flow.svg` (Flow — the EnsurePersonalBook sequence), `book-data-model-diagram.svg` (System — Book/BookMember/BookInvitation, with BookInvitation visibly marked unimplemented), `book-scoped-authorization-diagram.svg` (System — the real permission path for Account/Category/Tag contrasted with Transaction's still-uid-only path). Same visual language and security posture as Kıvılcım/DropSpot's diagrams (role="img", title/desc, no scripts/foreignObject/external resources/raster/animation/event handlers). The old generic placeholder was deleted.
- **Public ordering**: Kıvılcım → DropSpot → JointLedger → Professional Systems (D-016), confirmed on both `/` and `/work`. DropSpot's `nextSlug` changed from `professional-systems` to `jointledger`; JointLedger's own `nextSlug` is `professional-systems`. No link points at a 404 route.
- **TASK-004 impact**: 6 of 7 acceptance criteria now genuinely met (the D-016-order/upstream-disclosure criterion flips to met); the remaining criterion is permanently unsatisfiable as literally worded (its own premise — three specific *preview-depth* entries — no longer describes any state the site can be in, unaffected by JointLedger's publication). **TASK-004 is not marked COMPLETE.**
- **TASK-003 impact**: the D-016-order criterion (previously unmet solely because JointLedger was draft) now flips to met — 8 of 9 criteria met; the remaining one (homepage renders zero project images anywhere) is unrelated to project publication.
- **TASK-005/TASK-006 confirmation**: both remain COMPLETE, untouched this pass except the one explicitly-authorized DropSpot `nextSlug` content change.
- **Tests**: `tests/unit/jointledger-assets.test.ts` (34 tests, mirrors kivilcim-assets/dropspot-assets structure for the 4 diagrams) and `tests/unit/jointledger-content.test.tsx` (18 tests: schema, upstream disclosure via real `CaseStudyHero`/`ProjectCard` rendering, no-book-scoped/no-invitation-overclaim checks, required headings, layer-meaning gate, full publication-gate pass, Figures render, full template semantic rendering) — both new. `tests/e2e/work.spec.ts` gained a full JointLedger `describe` block (11 tests) mirroring Kıvılcım/DropSpot's, plus fixes to DropSpot's now-`jointledger`-pointing next-link test and removal of the now-meaningless "draft project" tests (zero draft projects remain anywhere in the repository — verified directly, `grep -l 'status: "draft"' content/work/*/index.mdx` returns nothing). `tests/e2e/home.spec.ts` updated the same way. `tests/unit/dropspot-content.test.tsx` updated for DropSpot's new `nextSlug`.
- Full verification suite: `pnpm typecheck` clean, `pnpm lint` clean, `pnpm format:check` clean (2 auto-fixes), `pnpm test` **312/312**, `rm -rf .next && pnpm build` succeeded (5 routes: `/`, `/work`, `/work/kivilcim`, `/work/dropspot`, `/work/jointledger`, `/work/professional-systems`), `pnpm exec playwright test` **164/164** (82 Chromium + 82 WebKit), `git diff --check` clean.
- Manual fresh-production-server checks (port 3107, torn down and confirmed stopped): exact route-status matrix; all 4 SVGs served as `image/svg+xml`; the old placeholder genuinely 404s; Kıvılcım→DropSpot→JointLedger→Professional-Systems order and next-links confirmed on both `/` and every case-study page; zero CONTENT REQUIRED/placeholder/credential/local-path/phase/aiAssisted leaks; upstream disclosure confirmed rendering on both the hero and the listing card.
- Visual review at all four required widths (375×812, 768×1024, 1024×900, 1440×1000) on `/`, `/work`, `/work/jointledger`: zero horizontal overflow anywhere. All 4 figures and the hero/card upstream disclosure individually captured and inspected — clean, on-brand, honest. All temporary screenshots, scripts, and both scratch clones deleted and confirmed gone.
- No new dependency was added (`git diff --stat` on `package.json`/`pnpm-lock.yaml`: empty). No new Client Component. `components/project/CaseStudyHero.tsx`/`ProjectCard.tsx` are the only component changes, both a narrow, already-mandated rendering fix (see above), not new design.
- No commit made. TASK-007 and later tasks not started.
- Status: **STOPPED — JointLedger published (`depth: "short"`, `verificationStatus: "verified"`) as TASK-004 content-system/publication scope. D-016 order complete for the first time (all four featured projects public). Most important honest limitation: transactions are not yet book-scoped, invitations are schema-only, zero frontend UI exists, and the work lives on an unmerged branch. Awaiting review.**

### 2026-08-10 — TASK-004 acceptance reconciliation; committed and pushed

- **Reconciled TASK-004's remaining criterion using the project's established supersession pattern** (same pattern as D-019's asset-criterion supersessions in TASK-003/005/006, but this reconciliation is task-acceptance-level, not a new architecture decision — `docs/DECISIONS.md` was not touched). Original criterion, preserved verbatim: "Published preview entries (kivilcim, dropspot, professional-systems) contain zero unverified claims — only approved seed/pending copy; professional pending copy is the exact D-009 wording." Its "three preview-depth entries" premise can never be literally true again: TASK-005 promoted Kıvılcım to `depth: "full"` (2026-07-31), TASK-006 promoted DropSpot to `depth: "short"` (2026-08-05), and this task's own JointLedger pass promoted JointLedger to `depth: "short"` (2026-08-10). Current replacement criterion: "all published project entries pass the publication gates required by their declared depth and contain zero unsupported or unverified public claims." **Independently re-verified against all four live projects before marking it met**: `validatePublicationGates` run directly against real frontmatter returned zero errors for Kıvılcım, DropSpot, JointLedger (all `verificationStatus: "verified"`, `factsCheckedAgainstRepo: true`) and Professional Systems (`"requires-user"`, `false` — correctly exempt from the full/short-only checks, zero concrete claims by design); zero `[CONTENT REQUIRED` markers in any of the four; all four routes return 200.
- **TASK-004 is now COMPLETE** — 7 of 7 criteria genuinely met, none weakened.
- **TASK-003 deliberately not force-completed this pass** — remains 8 of 9 met; its own D-016-order criterion stays `[x]` (already satisfied by JointLedger's publication), its D-019 image-criterion supersession note is untouched, and the remaining unmet criterion (zero project images anywhere on the homepage) is explicitly deferred to a separate pass before TASK-007, per this turn's own instruction.
- **TASK-005/TASK-006 confirmation**: both remain COMPLETE, untouched this pass.
- **Re-verified, unchanged from the prior pass**: JointLedger's metadata (`status: "published"`, `verificationStatus: "verified"`, `depth: "short"`, `factsCheckedAgainstRepo: true`, `provenance: "fork"`, `order: 3`, `phase: "active-development"`, `aiAssisted` omitted), the full upstream/contribution wording, the unmerged-branch disclosure, and all 4 `verified-diagram` SVGs (zero gate errors on a fresh `checkImageAssets` run). `CaseStudyHero.tsx`/`ProjectCard.tsx` re-confirmed: upstream rendering is driven entirely by `project.upstream` (no hard-coded project name), renders nothing for the three projects without that field, uses the existing `TextLink` external-link convention, and introduces no Client Component.
- **File-count discrepancy resolved**: the prior turn's final report said "Files modified (10)" but its own enumerated list actually contained 11 items — a header/count mismatch, not a missing or extra file. Independently re-enumerated via `git status --short`: **11 modified, 1 deleted, 9 untracked/new, 21 total paths** — matches exactly. `git diff --stat` shows only the 11 modified + 1 deleted = 12 tracked paths because `git diff` (no flags) compares the working tree against the index for already-tracked files only; the 9 new files were never added to the index, so they're structurally invisible to `git diff` until staged — this is standard git behavior, not evidence of a missing file.
- Full verification suite: `pnpm typecheck` clean, `pnpm lint` clean, `pnpm format:check` clean, `pnpm test` **312/312**, `rm -rf .next && pnpm build` succeeded (6 routes total, 4 case studies), `pnpm exec playwright test` **164/164** (82 Chromium + 82 WebKit), `git diff --check` clean — all exactly matching the expected baseline.
- Manual fresh-production-server checks (port 3108, torn down and confirmed stopped): exact route matrix; Kıvılcım→DropSpot→JointLedger→Professional-Systems order and full next-project chain confirmed with zero dangling/404 links; upstream disclosure confirmed rendering on both the `/work` card and the case-study hero; zero CONTENT REQUIRED/secret/credential/local-path/unsupported-claim leaks (specifically checked: no "transactions are book-scoped," no "invite a family/partner/member," honest "aren't book-scoped"/"Unmerged feature branch" language present). Zero horizontal overflow at 375/768/1024/1440px across all six routes (`/`, `/work`, and all four case studies). All temporary scripts and server logs deleted and confirmed gone.
- No new dependency added. No new Client Component. No product/content/UI change beyond this reconciliation's own scope — `content/work/jointledger/*` untouched this pass (no factual defect found).
- Committed and pushed to `origin/main` (see commit hash below). TASK-007 and later tasks not started.
- Status: **STOPPED — TASK-004 COMPLETE, committed, and pushed. TASK-003 remains at 8/9, deliberately deferred. TASK-005/006 unaffected. Awaiting review.**

### 2026-08-10 — TASK-003 homepage-image completion pass

- Closed TASK-003's last open criterion: the homepage previously rendered zero project images anywhere. `components/project/ProjectCard.tsx` (shared, unchanged, between `/` Selected Systems and `/work`) now renders each project's first registered `images[]` entry through the existing `Figure` component (same soft-paper mat, corner ticks, and mono caption used everywhere else on the site), wrapped in a `max-w-xs` (320px) container so it stays secondary to the title/description — one image-selection code path, not two, and no hard-coded slug in it.
- **No new content-model field**: used the existing `images[]` array's first entry as the representative asset, per this pass's own instruction ("use the first registered image unless a cover-image concept already exists" — none does). All three already-published projects' preferred homepage asset already happened to be their first registered image, so no reordering, no file duplication, and no change to any of their case-study asset order: Kıvılcım → `product-areas-map.svg` (`provisional-illustration`), DropSpot → `browse-drops.webp` (`real-screenshot`), JointLedger → `upstream-extension-map.svg` (`verified-diagram`).
- **New Professional Systems asset**: `public/images/projects/professional-systems/professional-systems-overview.svg` — a `provisional-illustration` built only from the project's own approved public description ("Designing usable interfaces within complex technical and organizational constraints."), depicting three abstract layer bands (Interface / Technical constraints / Organizational constraints) with no fake screen, dashboard, client, logo, metric, or confidential detail. Carries the required visible label "PROVISIONAL EDITORIAL VISUAL — NOT A PRODUCT SCREENSHOT". Registered in `content/work/professional-systems/index.mdx`'s `images[]` (previously empty). **Deviation from the suggested caption wording**: "Provisional editorial visual based only on..." was changed to "...editorial **illustration** based only on..." — the reusable `checkImageAssets` gate (D-019) requires a caption containing "diagram" or "illustrat...", which the originally suggested "visual" wording would not have satisfied; alt text needed no change (already contained "illustration"). A grammar-level substitution, not a fact change.
- **Observed, out of scope**: a stray, unreferenced `public/images/projects/professional/` directory (mismatched slug — missing the `-systems` suffix, from an earlier TASK-004 placeholder pass) still exists with an orphaned `placeholder-asset-pending.svg`. Confirmed via `grep` that nothing in `content/`, `app/`, `components/`, or `lib/` references it. Left untouched — not part of this task's scope, and removing it wasn't requested.
- **Tests**: `tests/unit/professional-systems-assets.test.ts` (20 tests, mirrors the kivilcim-assets/jointledger-assets structure for a single asset, plus extra checks for no invented metrics/clients). `tests/unit/project-card-images.test.tsx` (22 tests) — exercises the real `getProjectsByTier("featured")` loader and the real `ProjectCard` component via `renderToStaticMarkup` for all four live projects: exactly one `<img>` per card, correct real src/assetType per project, meaningful alt text, no `[CONTENT REQUIRED` marker, DropSpot's caption carries no false denial while the three diagram/illustration captions do, and a source-text assertion that `ProjectCard.tsx` contains none of the four image filenames or non-Kıvılcım slugs hard-coded (proving the selection is data-driven). `tests/e2e/home.spec.ts` gained 3 new tests (exactly one image per card capped ≤400px; DropSpot's real screenshot vs. the other three's honest diagram/illustration labelling; no fake-screenshot claim plus DOM-order proof that title text precedes the image). `tests/e2e/work.spec.ts` gained 1 new test confirming the same per-card image on `/work`.
- Full verification suite: `pnpm typecheck` clean, `pnpm lint` clean, `pnpm format:check` clean (1 auto-fix), `pnpm test` **354/354** (312 baseline + 42 new), `rm -rf .next && pnpm build` succeeded (6 routes, all prerendered), `pnpm exec playwright test` **172/172** (86 Chromium + 86 WebKit; 164 baseline + 8 new), `git diff --check` clean.
- Manual fresh-production-server checks (port 3109, torn down and confirmed stopped): exact route matrix (`/`, `/work`, all four case studies → 200; unknown slug → 404); D-016 order and every card's single representative image confirmed visually on both `/` and `/work` at 375×812, 768×1024, 1024×900, and 1440×1000 — zero horizontal overflow at any width (scripted `scrollWidth`/`clientWidth` check, all zero), images stay visibly secondary to title/copy at every width including 1440px (capped at 320px against a 1320px container), DropSpot's screenshot reads as a real screenshot, the three diagram/illustration cards keep their honest captions legible. All temporary screenshots and the temporary Playwright script (`.qa-screenshots.mjs`) deleted and confirmed gone from both the repo and the scratch directory.
- No new dependency added (`git diff --stat` on `package.json`/`pnpm-lock.yaml`: empty). No new Client Component. No case-study copy changed for Kıvılcım, DropSpot, or JointLedger — only Professional Systems' previously-empty `images[]` array gained one entry.
- **TASK-003 is now COMPLETE** — 9 of 9 acceptance criteria genuinely met, none weakened; the original criterion text and its D-019 supersession history remain intact and verbatim, with a further homepage-specific current criterion and its evidence appended beneath.
- **TASK-004/005/006 confirmation**: all three remain COMPLETE, untouched this pass.
- No commit made. TASK-007 and later tasks not started.
- Status: **STOPPED — TASK-003 COMPLETE (9/9). Awaiting review.**

### 2026-08-11 — TASK-007 Motion layer + interactive explorer implemented

- **Started from substantial pre-existing, uncommitted work**, not a clean slate: the working tree already contained `motion` installed, `components/project/LayerExplorer.tsx`, `components/ui/motion/{Reveal,PanelTransition}.tsx`, and reveal wiring across most homepage sections — closely matching this task's own file plan. Read every changed/new file in full before touching anything, confirmed the pre-existing work was high quality and consistent with the task's binding constraints, and continued it rather than discarding or restarting it.
- **Real bug 1 (blocking, found via `pnpm lint`)**: the classic `useState` + `useEffect(() => setMounted(true), [])` hydration-gate pattern, used identically in both `Reveal.tsx` and `LayerExplorer.tsx`, is flagged by this project's `react-hooks/set-state-in-effect` lint rule as a cascading-render anti-pattern. Fixed by extracting a shared `lib/utils/useHasMounted.ts` (`useSyncExternalStore`-based — the rule's own suggested alternative), meeting ARCHITECTURE §17's "two or more real places" bar for a new `lib/utils/` primitive.
- **Real bug 2 (blocking, found empirically via a browser opacity trace, not by reading code)**: `useHasMounted`'s first implementation used a no-op `subscribe` function that never actually notified React to re-check the client snapshot — `mounted`/`enhanced` stayed permanently stuck at the server (`false`) value on a real hydrated production page, even though the exact same code reported `true` immediately inside Vitest/RTL's `render()` (which uses `createRoot`, never consults `getServerSnapshot`, and therefore could not have caught this). Real symptom observed: the Layer Explorer never actually became interactive in a real browser, and the homepage carried a permanent duplicate "Surface"/"Flow"/"System" heading (the static definitions list *and* the never-enhanced stacked explorer). Fixed by making `subscribe` schedule a real `setTimeout(callback, 0)` notification.
- **Real bug 3 (blocking, found by an independent adversarial review + confirmed with a second, non-scrolling browser opacity trace)**: with bug 2 fixed, a second, more serious race remained: `useHasMounted`'s mount-flip and Motion's `useInView` hook resolve on two independently-scheduled async paths, and `useInView` always *starts* `false` regardless of whether the element is already on screen. For any content not yet scrolled to (the large majority of the homepage), `mounted` reliably resolved before `inView` did, so `Reveal`'s `animate` target flipped from the honest pre-hydration `visible` state to `hidden` — and because Motion animates *any* change to `animate`, not just the first mount, real content visibly faded to invisible the instant JavaScript finished loading, before the user had scrolled anywhere near it. A trace with no scrolling at all showed every below-the-fold `Reveal` (and, worse, the above-the-fold Positioning statement too) genuinely sitting at `opacity: 0`. Fixed two ways together: (1) the transition *to* `hidden` is now instant (`duration: 0` — a single imperceptible state change, not an animation), while the transition *to* `visible` still animates normally, so only the legitimate reveal-on-scroll (or reveal-on-load) case ever looks like a "reveal"; (2) content empirically confirmed to always sit inside the initial viewport (Hero, Positioning) is additionally marked `onLoad`, skipping the async observer entirely as belt-and-suspenders. Re-traced after the fix: zero in-viewport elements ever observed at `opacity: 0`.
- **Historical bug re-confirmed still fixed**: an earlier version of `Reveal` (present before this session started reading the pre-existing work) conditionally rendered either a plain `<div>` or a `motion.div` depending on mount state, which silently broke `useInView`'s one-shot observer across the element-type swap. The current code always renders `motion.div`, unconditionally — re-verified, not assumed.
- **Adversarial review**: three independent, parallel code reviews (accessibility/keyboard, reduced-motion/no-JS completeness, DESIGN_SYSTEM/CLAUDE.md compliance + scope discipline) were run against the implementation before finalizing. Combined findings, beyond the three bugs above: (1) tab buttons had `min-h-11` but no width guarantee — fixed with `min-w-11`; (2) `tabpanel` elements had no `tabIndex={0}` fallback, so a keyboard user could Tab straight past a panel with no focusable content — fixed; (3) clicking a tab only updated React state, never real DOM focus — a genuine WebKit/Safari gap (Chromium focuses buttons on click, WebKit does not) — fixed by calling `.focus()` in `activateTab`; (4) `LayerExplorerIntro`'s real project-preview block (the "Previewing real layers from…" line + the explorer itself) had no `Reveal` treatment at all, unlike every other homepage section's primary content — fixed; (5) `PanelTransition` animated its enter transition unconditionally, including the automatic first mount hydration itself causes (re-showing already-visible Surface content with a needless fade) — fixed by only animating on a genuine, user-triggered tab switch (tracked via a new `hasSwitchedTabs` state in `LayerExplorer`, threaded through as `PanelTransition`'s `animateEntry` prop); (6) the mobile tablist had no `overflow-x-auto` safety net for a translation-ready label set — added defensively, matching DESIGN_SYSTEM §12's literal wording, though not currently exercised by the English labels. Two findings were reviewed and confirmed **not** defects: the `ProjectCard` hover/focus border (flagged as possible scope creep against the persisted docs alone) is directly authorized by this turn's own "Project-card interactions" instructions; the panel-swap using an enter-only fade rather than a literal dual-panel crossfade was a deliberate, disclosed choice (a true `AnimatePresence` crossfade would keep two `tabpanel`s simultaneously un-hidden from assistive tech for the transition's duration, in tension with WAI-ARIA's "expose one panel at a time" convention) — kept as-is.
- **Homepage motion audit completed**: `HowIBuild.tsx`, `FieldNotes.tsx`, `AboutPreview.tsx` had no reveal treatment at all before this pass (only `Hero`/`Positioning`/`LayerExplorerIntro`/`SelectedSystems`/`BuiltForRealLife` did) — each now gets one section-level `Reveal` around its primary content block, matching the established one-`Reveal`-per-section pattern. All 8 PROJECT_SPEC §7 homepage sections now have a deliberate, documented motion treatment.
- **Tests**: `tests/unit/layer-explorer.test.tsx` (10 new RTL tests — the first real `@testing-library/react` `render()`/`userEvent` usage in this codebase, per ARCHITECTURE §10's own plan; required adding `afterEach(() => cleanup())` to `tests/setup.ts` since `vitest.config.ts` doesn't set `test.globals: true`, so `@testing-library/react`'s automatic cleanup detection never registered — without it, RTL's rendered trees silently accumulated across tests in the same file). `tests/e2e/motion.spec.ts` (new — Hero/reveal visibility, homepage explorer preview, keyboard/touch/focus, no-JS, `reducedMotion: 'reduce'` emulation, responsive). Six pre-existing e2e tests (in `home.spec.ts`/`work.spec.ts`) needed updates to reflect **intentional, approved** new behavior this task introduces, not to paper over a regression: (a) the Layer Explorer preview adds a genuine 5th `/work/` link on the homepage (the "Previewing real layers from…" link), so a bare `main a[href^='/work/']` count assertion needed scoping to the Selected Systems list specifically; (b) Surface/Flow/System are now interactive tabs on case-study pages, not simultaneously-visible headings, so tests asserting all three headings/images visible at once were rewritten to click through each tab; (c) a `boundingBox()`-based aspect-ratio test had a real, pre-existing image-load timing flake, fixed with `expect.poll`.
- Full verification suite: `pnpm typecheck` clean, `pnpm lint` clean, `pnpm format:check` clean, `pnpm test` **364/364** (354 baseline + 10 new), `rm -rf .next && pnpm build` succeeded (6 routes), `pnpm exec playwright test` **224/224** (112 Chromium + 112 WebKit; 222 baseline-after-fixes + 2 net new from splitting one test into two).
- Manual fresh-production-server checks (torn down and confirmed stopped): D-016 order, all four representative images, JointLedger upstream disclosure, DropSpot real-screenshot labelling, next-project chain, and D-019 honesty across all three Layer Explorer tabs all re-confirmed intact; tab touch targets measured ≥44×44px; `tabpanel` keyboard-reachable; click-to-focus sync confirmed; zero console errors/warnings/hydration issues across all 9 routes with the explorer exercised on each; zero horizontal overflow at 375/768/1024/1440px; a direct `curl` of raw server HTML (zero JavaScript) confirmed real stacked `<h2>Surface/Flow/System</h2>` headings and zero `role="tab"` occurrences on both `/` and `/work/kivilcim`. All temporary QA scripts and screenshots deleted and confirmed gone.
- **Regression gates**: all four projects remain public in D-016 order; the full next-project chain is intact; JointLedger's upstream disclosure still renders; all four representative images still render exactly as TASK-003 left them; DropSpot's real-screenshot labelling is unchanged; D-019 honesty holds across every Layer Explorer tab on every case study. TASK-003/004/005/006 all remain COMPLETE, untouched.
- Only `motion` added as a new dependency (confirmed via `package.json`/`pnpm-lock.yaml` diff). No content changed (`git diff` over `content/` is empty). No commit made.
- Status: **STOPPED — TASK-007 implemented, 7/7 acceptance criteria genuinely met after fixing three real bugs (one lint-caught, two empirically-caught) and five adversarial-review findings. Awaiting review.**

### 2026-08-11 — TASK-007 reviewed and approved; finalized

- **TASK-007 reviewed and approved.** This entry records the finalization pass: no interaction, motion, visual, content, architecture, or dependency changes were made — only re-verification, documentation, commit, and push.
- **File-inventory discrepancy from the prior report, resolved.** The prior report's "6 created paths" and "5 new (`??`)" were both correct readings of different things, not a real inconsistency: `git status --short`'s default mode collapses an entirely-untracked directory into a single `??` line, and `components/ui/motion/` (untracked) contains two files (`Reveal.tsx`, `PanelTransition.tsx`). Confirmed with `git status --porcelain --untracked-files=all -- components/ui/motion/`, which correctly expands it to two `??` lines. `git ls-files` on all six previously-reported "created" paths returned nothing for every one of them — confirming all six are genuinely new (untracked before this task), none were tracked-and-modified. Exact reconciled counts: **19 modified, 6 new, 0 deleted, 25 total working-tree paths** (24 `git status --short` lines, because one line represents 2 files).
- **Architecture budget re-confirmed.** `grep '"use client"'` across `app/`, `components/`, `lib/` returns exactly four files: the three approved TASK-007 additions (`LayerExplorer.tsx`, `Reveal.tsx`, `PanelTransition.tsx`) plus the pre-existing, untouched `components/layout/MobileNav.tsx` (confirmed absent from `git status --short` and from `git diff --stat`, both before and after this finalization pass).
- **Interaction contract, no-JS output, and reduced-motion behavior re-read directly from the current source** (not assumed from memory): `LayerExplorer.tsx` still implements manual-activation WAI-ARIA Tabs exactly as documented (Arrow keys move focus only, Home/End to first/last, native Enter/Space activation, roving `tabIndex`, `aria-selected`/`aria-controls`/`aria-labelledby`, click syncs real DOM focus, `tabpanel` carries `tabIndex={0}`, tab buttons carry `min-h-11 min-w-11` — 44×44px CSS confirmed); `Reveal.tsx`/`PanelTransition.tsx` still contain the asymmetric-duration fix and the `animateEntry`/`onLoad` gating exactly as previously verified.
- **Full quality suite re-run clean from the final working tree**: `pnpm typecheck`, `pnpm lint`, `pnpm format:check` all clean; `pnpm test` **364/364**; `rm -rf .next && pnpm build` succeeded (Turbopack, 11 static/SSG routes including all 4 case studies); `pnpm exec playwright test --list` confirmed **224 tests** across 5 spec files; `pnpm exec playwright test` **224/224** (112 Chromium + 112 WebKit) — identical counts to the prior pass, no drift. `git diff --check` exit 0. One benign, pre-existing `[WebServer] Error: Internal: NoFallbackError` line appears twice in the Playwright run output (Turbopack's own dev-server log for the intentional "an unknown slug 404s" test in each browser project) — traced to the exact test by log position; it is server-side stderr noise from an intentionally-requested nonexistent route, not a browser console error, not a test failure, and not a TASK-007 regression (the test itself predates this task).
- **Fresh production server verification**: all 10 required routes matched the expected matrix exactly (`/`, `/work`, all 4 case studies, `/about`, `/notes`, `/lab` → 200; unknown slug → 404). A temporary Playwright spec exercising all 9 routes plus every Layer Explorer tab (click-through + keyboard walk) on both `/` and `/work/kivilcim`, on both Chromium and WebKit, recorded **zero** console errors, zero console warnings, and zero uncaught page errors. Deleted after use.
- **No-JS re-confirmed via direct `curl` of raw server HTML** on the fresh production server (not Playwright's JS-disabled emulation this time, the actual unprocessed bytes): both `/` and `/work/kivilcim` contain real `<h2>Surface</h2>`/`<h2>Flow</h2>`/`<h2>System</h2>` headings, correctly ordered, and exactly **zero** `role="tab"` occurrences.
- **Visual QA re-run at all four required widths** (375×812, 768×1024, 1024×900, 1440×1000) against `/work/kivilcim` with Surface/Flow/System each activated in turn, plus keyboard-focused-tab bounding-box checks (never clipped outside the viewport) and zero-horizontal-overflow assertions — all passed. One investigative finding, resolved as a non-issue: a `fullPage: true` homepage screenshot at 375px showed large unexpected blank gaps between sections; a follow-up check proved this is a **screenshot-capture artifact**, not a real bug — a script that scrolled the real page incrementally (simulating an actual user) and read every `[data-reveal]` element's computed `opacity` found **zero** elements below `1`, while a `fullPage` screenshot taken immediately after a fresh page load (no manual scroll) caught most of the page still mid-transition because Playwright's full-page capture resizes the viewport and screenshots before the resize-triggered `IntersectionObserver` callbacks and their React re-renders have settled. This matches an already-documented quirk from earlier in this project (the same mechanism previously produced false-positive overflow numbers). All temporary screenshots and specs deleted after use; production server process confirmed killed (`curl` to `localhost:3000` afterward: connection refused).
- **Dependency re-confirmed**: `package.json` diff is exactly the one line adding `"motion": "^13.0.0"`; the `pnpm-lock.yaml` diff adds only `motion@13.0.0`, `framer-motion@13.0.0`, `motion-dom@13.0.0`, `motion-utils@13.0.0` — all inside `motion`'s own resolved dependency tree (`tslib`, which `framer-motion` depends on, was already present in the lockfile before this task, confirmed by checking `HEAD`'s copy). No other runtime or dev dependency changed. No unrelated path found in the working tree; scope confirmed clean before staging.
- TASK-007 marked **COMPLETE** above (was "implemented/awaiting review"); all 7 original acceptance criteria remain verbatim and `[x]`, each with its original evidence note preserved, plus this pass's re-verification summary appended to the task's status line. TASK-004/005/006 confirmation unchanged (no earlier completed task reopened). TASK-008 was not started — no SEO/a11y/performance-scoped file touched.
- Status: **TASK-007 COMPLETE. Committing and pushing per this turn's explicit instruction.**
